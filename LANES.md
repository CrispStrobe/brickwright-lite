# Who is doing what — claim before you start, release when you finish

Created 2026-08-24, after two sessions independently did the same repair.

## Why this file exists

The vendor of `sb3-creator eac7a1a` made two OPEN DEFECT sentinels fire — the
shift-register bit test and the four faceplates with dead controls. Both are
written to go red the moment their defect is fixed, and both carry instructions
for what to do when that happens. Two of us followed those instructions, at the
same time, without knowing about each other. The work was done twice, reached
`main` twice, and had to be merged against itself.

Nobody ignored a rule; there was no rule. The coordinating session worked from a
worktree created *before* the other landed, and never re-checked. A worktree is a
photograph of `main`, and the longer you hold it the more of a lie it becomes.

## The protocol

**1. Before you start anything, look.**

```bash
git fetch origin
git log --oneline -20 origin/main          # what landed while you were away
git branch -r --sort=-committerdate | head # what someone else has open
```
Then read the CLAIMS table below. If your work is already claimed or already
landed, you have just saved yourself an hour — say so and pick something else.

**2. Claim it here, in the same push as your first commit.**

Add a row before you begin. A claim costs one line and is the only thing that
makes a collision visible *before* both of you have done the work.

**3. Release it here when you finish**, in the same push as your last commit.
Move the row to DONE with the sha. An abandoned claim is worse than no claim:
the next person reads it as work in progress and stays away from something
nobody is doing.

**4. Sentinels are claimable work too.** A test whose name begins `OPEN DEFECT:`
is a standing instruction that fires later, possibly for someone who did not
write it. If one goes red for you, CLAIM IT before acting — that is exactly the
case this file was written for.

**5. If you push something that changes what another repo's gates load** — a
pin, a vendored file, a shared fixture — say so in the same minute, in the row.

**6. Before pushing a ledger edit, check the SHAPE of your tree, not just
your diff.** On 2026-08-24 main's entire tree was wiped (84,148 files,
12.6 M deletions) by a single LANES commit made from a one-file working
copy — and the NEXT session then inherited the wipe by rebasing onto the
wiped tip: its own commit read `1 file changed, 1 insertion(+)` and
nothing looked wrong, because a rebase onto an empty tree silently makes
your commit an edit OF that empty tree. So neither "pull first" nor
reading your own diffstat catches this. One command does, at every point
it passed through:

```bash
git ls-tree HEAD | wc -l     # a single entry means your tree is a DELETION, not an edit
```

Run it before pushing a ledger row, and ESPECIALLY after any rebase —
rebasing onto a broken tip is how a wipe propagates to people whose
checkouts were fine. Same failure species as a detector confidently
reporting a number about the wrong thing: the diffstat was true, it was
just about the wrong thing.

A count that drops means LOOK, not PANIC — the count flags candidates,
the name-level diff gives verdicts:

```bash
comm -23 <(git ls-tree -r --name-only <before> | sort) \
         <(git ls-tree -r --name-only HEAD | sort)      # names missing now
```

(Post-restore audit of the 2026-08-24 wipe used exactly this: tree
counts differed across tips by ordinary commit traffic, which looked
like a second smaller loss and wasn't; the `comm` against the last
pre-wipe tree — zero missing, zero spurious, non-ledger diff empty — is
what established "nothing was lost" at content level, not count level.)

None of the above sees a rollback WITHIN this file — the file stays
present and non-empty while rows vanish. The same incident had that
too: a second, parallel restore carried a stale LANES.md and silently
dropped four rows (three DONE — exactly the invitation to redo landed
work that rule 3 warns about), and it stayed invisible for four commits
because every later edit built on the rolled-back base. So: a restore
that touches LANES.md must be a SUPERSET check against the tip it
replaces (`comm` the non-blank lines both ways — zero missing), never a
checkout of an older copy. And when you verify a repair, say WHAT your
check is scoped to: the audit that reported "nothing was lost" here had
excluded LANES.md — right for auditing the tree restore, wrong as a
statement about the repository, and scoped to exclude the one file that
was actually damaged. A check scoped away from the likely damage always
comes back clean.

The superset rule has ONE exception, and missing it put three rows in both
tables at once (`7925e95af`, "restore three rows my union resolution dropped").
**A row that MOVED from CLAIMS to DONE is not a row that was dropped.** Restore
by lane NAME, not by line: if a lane already has a DONE row, its CLAIMS row is
finished work, and re-adding it resurrects a lane everyone else believes closed.
Check both tables for the lane name before restoring anything.

## CLAIMS — work in progress

| lane | who | started | what |
| --- | --- | --- | --- |
| measured thresholds: sb3-creator half — phase 2 | UNCLAIMED (parked by bw-audit 2026-08-25) | — | **Park-state, written so a fresh session can resume without re-deriving anything.** Phase 1 is in DONE below. **What remains:** (a) **3 targets unprobed**, all in the two files that import `globSync` from `node:fs` — `example-corpus-contract.test.mjs` and `generated-bench-layout.test.mjs`; (b) **25 of the 28 remaining can-fire'd floors have no bisected margin** — only kcl-residual:101, flat-variants:92 and expected-quantities-hold:148 were bisected, at ~2.5 min each; (c) **~178 literals below the >=100 cut are untouched** — the sub-100 floors plus all 18 timeout-ms, 11 ceilings, 7 tolerances, 4 size-caps, 3 pins, 2 concurrency, 2 timeout-min. **THE RIG IS BUILT AND KEPT: `/mnt/volume1/code/wt/rig-thresholds`** — both siblings cloned at the PINNED shas (`bw-board a301937`, `bw-circuit-ui af5cc08`), bw-board's deps installed from its own lockfile, 61 MB, nobody else's checkout touched. Drive the probe with `BW_BOARD=$RIG/bw-board BW_CIRCUIT_UI=$RIG/bw-circuit-ui node scripts/threshold-probe.mjs --file <f> --line <n> --gate <f> [--margin]`. **A SECOND RIG AXIS, easy to miss:** the runtime. This box is node 20.20.2, CI pins **22**, and the two `globSync` files cannot be IMPORTED on 20 — module-load failure, not an assertion. I fetched `node-v22.11.0` to a SESSION scratchpad (so it is gone now) with its SHA-256 checked against nodejs.org's `SHASUMS256.txt` first; re-fetch it the same way, because a toolchain fetched by name is the same defect as a corpus fetched by name. **Method note that saved the most time:** a corpus floor's slack is directly countable without any rig, and the three bisections confirmed that for a `>=` floor over a countable corpus the flip point IS the count — so count first and probe only for can-fire. Full write-up in sb3-creator `docs/MEASURED-THRESHOLDS.md`. |
| lite sb3-creator pin + the stale gallery | DONE (bw-bundle's catch, landed by the Mac fable session in c52c1c00e) | 2026-08-25 | **RESOLVED by coordination** (message stuffed into bw-bundle's window before landing): sb3-creator main had moved past 73d3174 to 649fdda (the ADC/PWM landing), so the Mac session — which caused the staleness in ba02583ef and had the newer trees staged — landed pin `649fdda` + all 43 gallery files + rebuilt build mirror in ONE commit, both `--check` guards green. bw-bundle's catch was correct and fast; credit stands. FOURTH sighting of the gallery-lags-pin species — and a new wrinkle recorded here: a bare `npm run sync:examples` (remote mode) once completed silently WITHOUT syncing while its exit code went unchecked behind a pipe; always run it `--dir` against the local checkout being pinned, and check the exit code, not the silence. Original claim text follows for the record. Bumping lite's `sb3-creator` pin to main tip `73d3174`. **The pin was already at `b27625b` when I looked — but the EXAMPLES GALLERY was not synced with it.** `sync-examples.mjs --check` against lite's OWN current pin reports **43 stale files**, including 78-a2-calculator (still two `seven_seg_4` in lite while `b27625b` has the `sevenseg8` overhaul), 71-calculator-pcb, eater6502-bench's rom.bin and 38 EXPECTED.md provenance stamps. So the transpiler tree moved and the gallery did not. Not a hole in the gate: `vendor-freshness` runs exactly this check, and the run for the pin-bump commit `ba02583ef` is still QUEUED — it would have gone red when it ran, and this closes it before it does. Syncing BOTH trees to one sha so the pin and what it names agree. No deploy marker. |
| labwired evaluation (oracle + wasm core) | **ADAPTER LANDED 2026-08-27 (bw-board fbd1086); netlist bridge still UNCLAIMED** | 2026-08-27 | **THE BOUNDARY-A ADAPTER IS DONE AND TESTED AGAINST THE REAL ENGINE** (`bw-board/src/labwired-adapter.js` + `test/labwired-adapter.test.mjs`, 6/6): labwired-wasm built from the pinned fork (94 MB raw → 20 MB after wasm-bindgen, matching the spike's 19.55 MB), loaded under `node --test`. The mapping: `watch_logic_signals` arms capture AND returns every ref's level (pillar-1 seating for free), `read_logic_edges` yields `{ch,cycle,value}`, `pin_routing` gives the pad MODE from register truth, `set_board_io_input` takes inputs back — and because that last one only resolves manifest-declared ids, the adapter GENERATES the system manifest (one input binding per header pin), which is the boundary-A-sized slice of the netlist bridge. **TWO SILENT TRAPS, both now written down in the code:** (1) `serde-wasm-bindgen` returns JS **Maps**, not objects — `batch.cursor` reads `undefined` and `JSON.stringify` prints `{}`, so an adapter written to the documented field names seats every pin low and reports the firmware never moved (it had: `step_batch` returned its full cycle count and the PC advanced; only the readback was blind). (2) **`type: stm32_gpioport` routes to labwired's STM32F1 register map** (CRL @0x00, ODR @0x0C, BSRR @0x10) — the F0 is V2 (MODER @0x00, ODR @0x14, BSRR @0x18), so every output write hit a different register and every pad read low forever while the firmware ran and the UART talked. Proven by discrimination: a firmware driving PA0 via ODR and PA1 via BSRR showed both dead AND reported PA1 `analog` for a MODER value meaning `output` — exactly F1 CRL decoding. `config: {profile: stm32v2}` fixes it. **Our oracle could not have caught this**: it compares UART bytes and reassembles edges from raw BSRR *word writes*, both layout-independent — the adapter is the first thing that ever asked labwired for a pad LEVEL. Upstream's own onboarding configs (stm32f0/f072/f4/f746/h743) share the shape — an upstream issue worth filing, with the reproducer. Oracle re-run against the corrected fixture is UNCHANGED (10 edges @ 240015 cycles/half-period, 47.999-of-48); bw-board suite 2732/0. **REMAINING for Phase 2:** their board manifests → our circuit model in general, and the lite wiring (a sync script + lazy chunk, the emu8051-wasm pattern). Prior status follows. **CORRECTED (owner's catch): labwired is NOT a 2026-08-25 find — the fleet adopted it before** (`sb3-creator/reference/corpus-and-oracles.md`: RP2040 second-opinion oracle, layer-5 sweep; `stc/docs/ROADMAP.md` ~2026-08-14: 'STM32 via the labwired-core adoption already seeded: G0/F103 before F4; never hand-rolled', validation rows landed — nRF52840 silicon-verified, ESP32-C3 reset-state-only). What IS new from 2026-08-25: the API-verified current state (stm32f103 conformance + fuzz crates, M0+/M3/M4/M7/M33 + RISC-V + Xtensa, Fidelity Ledger) and the WASM question. **OPEN TENSION for the owner:** the stm32 phase-1 lane hand-rolled an F030 machine against that 'never hand-rolled' line — the two honest reconciliations are in bw-board `STM32-PATH.md` Phase 4 (F0 path ships now + becomes labwired's validation baseline, or roadmap enforced strictly and the F0 machine demotes to test oracle). The codegen, compile target, and chain tests survive either way. **Meta-lesson for every lane** (this is the third sighting of the species: stc-b6's remote-ref rule, the UNO alias walk, now this): scoping starts with a grep of OUR OWN .md corpus across all repos — external verification without internal search re-derives or contradicts recorded decisions. Work items unchanged: (1) labwired as the F0/F103 differential oracle; (2) the wasm-core evaluation — now with the roadmap's G0-before-F4 ordering attached. |
| stm32 path: phase 1 (F0 board) | bw-engine (lite-61, Mac) | 2026-08-25 | **BOTH technical halves are DONE and chain-proven**: the emulator half (bw-board master — F0 board on the Phase-0 machine, level-triggered park wake after the pended-before-WFI trap froze time at 0.38x) and the codegen half (sb3-creator main — `DEVICE STM32F030`, RM0360 vocabulary behind a `_cStm32` flag riding the 'arm' core the way `_cTiny88` rides 'avr'; real vector table, TIM3 tick, WFI idle, USART1 print, and a LEAK GUARD: any RP2040 register surviving into an F0 build warns by name — unported features refuse loudly). `test/stm32f0-chain.test.mjs` runs blocks → generateC → real gcc → the F0 machine: blink on the 500 ms grid, pull-up button, >90% parked, both honesty ledgers empty; idle roster now 16 chips; 223/0 across affected suites. Three found-by-running traps recorded in the commit messages (the 125 MHz clock leak = exactly 0.384x time; the level-wake; the leak guard). **stc-compiler target LANDED** (main: `stm32f030` in ARM_TARGETS, its own flash linker script with KEEP(.vectors) first, per-spec origin, the main-at-origin check gated pico-only; the new service test decodes the image and asserts the SP/reset boot words — service CI validates with the real toolchain). Owner's Blue Pill references folded into STM32-PATH.md Phase 2 (board truth, the GPL-oracle framing for nviennot's emulator — RUN for differential comparison, never linked or shipped — and a difficulty-ordered test-firmware corpus with the rebuilt-as-M0 caveat per entry). **LITE WIRING DONE (lite ba02583, 2026-08-25): STM32F030 SHIPS end to end.** Owner ruling recorded in STM32-PATH.md: **two tiers permanently** — the hand-rolled machine is the LIGHT tier, peripheral set CAPPED at the codegen vocabulary; labwired beyond. What landed: bw-board c838fb1 (boundary-A `stm32-adapter.js` in the shared adapter-contract roster with a hand-assembled Thumb blink; the rp2040js debug target REUSED unchanged through the adapter's bus+sram+clock facade — zero new stepper; machine grew tickNs with the level-wake rule so the debug loop can't sleep through a pended-before-WFI interrupt; `stm32f0` factory kind, roster 10). Lite: device picker (STM32 (ARM) group), compile path (target stm32f030, format bin, image stays BYTES — a real flash image the machine boots like silicon), attachStm32F0Target at 48 MHz, debug-panel kind maps. The deep-import problem solved in sync-bw-board (rewrites the machine's rp2040js core path to one that resolves from the vendored location — the exports map hides cortex-m0-core; NO webpack alias needed). 1036 lite node tests green; bundle greps confirm the picker label. **DEPLOY GATE (not this lane's to fix): stc-compiler a82c5b7 is pushed but UNSERVED — Vercel's 100/day account cap; its git integration is now DISCONNECTED (owner ruling, kerotakis pattern: pushes free, deploys deliberate via scripts/deploy-vercel.sh --prod). Until someone runs that after the cap resets (~2026-08-26), Run on STM32F030 answers 'unknown target' honestly. HF Space mirror prepared but GATED — HF now wants PRO for Docker Spaces.** **ADC + PWM LANDED AND VENDORED (bw-board 6373ef6, sb3-creator 649fdda, lite c52c1c00e)**: the cap vocabulary is complete — RM0360 ADC (channel = PA pin, parse- and emit-time refusal elsewhere, MODER 11-analog, RCC in the computed APB2 write) and TIM3-compare PWM riding the existing 1 kHz tick frame (PA6/PA7/PB1 via AF1, zero extra timers; a frozen OCxM publishes NOTHING). Chain-proven from blocks: mid-rail pot prints 2047/2048 over USART, 30% asked = 30.0% measured at the pad. Found-by-running: the machine used to advance peripherals BEFORE stamping time, which stamped every parked PWM edge one park early and would have SWAPPED an asymmetric duty at the board (30%→70%) — order flipped in all three advance paths. **ALL LEVELS COMPLETE (lite a41829f1d, owner directive "bring this up all levels")**: designer part (bw-circuit-ui 06c7ea7 — TSSOP20 sidecar+SVG+DIP-20 footprint, 3.3 V seated bench), engine board kind (bw-board 1caf851), retarget axis (sb3-creator 9aef707 — 95 gallery examples offer the F030 with seated benches; tier-cap refusals worded as reasons), stc/docs/ROADMAP two-tier ruling recorded, STM32-PATH Phase 1 stamped shipped, test-api gains the stm32f030 production case for the post-deploy run. **The recompute repaired two dormant retarget species** (pins DECLARED PWM or TONE refused on EVERY device while the index still offered ten — fade family restored everywhere, tone family returned to the 8051s, 86 orphan benches removed, 07-buzzer-siren's falsified refusals deleted); lite's corpus count gate moved 1094→1170 WITH the gallery (the da44ea10a lesson applied). Cross-lane: stc-76 (PCB, bw-circuit-ui fable/pcb-support) was unblocked (its ratchet red was a pre-bump branch — bbcdb3a already recorded slide_switch 24→25 for 71-calculator-pcb) and got the F030's 1..20 terminal walk for its PAD_TERMINALS lift. **THE LABWIRED ORACLE IS RUNNING (bw-board 31ff578)**: `test/labwired-oracle.test.mjs` — the same gcc-built RM0360 ELF on our CortexM0Machine+F0 board AND labwired-core's stm32f0 model (test mode), UART byte streams byte-identical, gated on `LABWIRED_CLI` + gcc. Three first-contact findings in the commit: their F0 onboarding yaml wires NO IRQs (fixture copy adds timer3 irq:16), their `--vcd` omits 32-bit peripheral writes (write_u32 never notifies observers — candidate upstream issue), and their cycle counter under WFI fast-forward is ~2.48M cycles/100ms-phase vs our 4.8M at 48 MHz (content agrees, timebases differ — ledgered). **The sb3-creator CI red a VPS session flagged is FIXED (917a0fc)**: the 9aef707 corpus predated the SIBLING PINS — bumped to bw-board 1caf851 + cui 06c7ea7, validated 6609/1→green against a worktree AT the pinned shas, EXPECTED provenance restamped (31 files, numbers unchanged). **Both pins deliberately stop BEFORE bw-cui2's attiny88 rename** — its re-seat inheritance is intact, per its claim. **NIGHT CLOSE (owner: 'go on coding, testing, against oracles, with real firmwares'):** (1) THE COMPILE SERVICE IS LIVE — the Vercel window reopened early, scripts/deploy-vercel.sh --prod succeeded, production serves target stm32f030 with correct boot words; test-api's post-deploy run then caught a PRE-EXISTING production 500 (translate-project read req.symbols, a field ProjectReq never had — every linked Keil project 500'd) — fixed+pushed (stc-compiler 0dfc8d9), deploy rides the next window (cap re-closed behind us). (2) THE FULL USER PATH IS PROVEN IN THE GUI: a headless probe drives the deployed app (Code tab → paste DEVICE STM32F030 program → ▶ Run) — it caught the last broken link (attachStm32F0Target missing createDebugSession in its import destructuring; lite c020d1838) and now reaches 'running' with the pin table live (pot at 1.65 V). (3) THE ORACLE GREW TEETH: labwired FORKED (CrispStrobe/labwired-core, branch bw/mmio-write-observers — SystemBus write_u32/u16 peripheral branches now notify observers, Machine.advance mirrors machine.observers into bus.observers; upstream's --vcd recorded pc and nothing else), and the oracle's second test reassembles PA0's edge timeline from the forked VCD's BSRR byte events: sequences agree edge-for-edge and the cross-simulator timebase ratio measures **47.999 cycles/us against the asserted 48** (bw-board aae3727). The harness's own first catch is documented in the test (VCD prints changes only; we=1 held across a word's byte events dropped every CLEAR edge). Upstream PR candidate: the two-line observer fix. **SECOND WAVE (owner: 'do it all' / 'use our fork'):** (a) **The fork is the fleet's labwired** — CrispStrobe/labwired-core. **RE-PINNED 2026-08-26: @ main = upstream 41119903c** (fork synced, default branch moved back to `main`). Our PR w1ne/labwired-core#1067 was SUPERSEDED by #1068, which is MERGED upstream and keeps the commit and co-authorship. **Do not re-pin bw/mmio-write-observers (5b7461d): it aborts the simulator** — `write_u16`'s peripheral branch carried a copy-pasted 4-byte notify loop beside its correct 2-byte one, and shifting a u16 by 16/24 overflows under that workspace's `overflow-checks = true` + `panic = "abort"` in both profiles, so any 16-bit store to a clocked peripheral died (an ordinary TIMx_ARR write suffices). Our F0 oracle never tripped it only because CMSIS makes TIM3's registers `__IO uint32_t` — 32-bit stores took the correct write_u32 path. The merged shape derives the byte count from the stored type (`notify_peripheral_store(addr, &value.to_le_bytes())`) and fixes registration at `Machine::add_observer`/`SystemBus::add_observer` rather than mirroring by LENGTH in advance() (which evicted the DAP memory tracker). Re-verified against the new pin: bw-board 15bc626 built labwired-cli from it and both oracle tests pass, pin-edge NOT skipped, same 47.999-of-48 story. (b) **The wasm-spike gate is PASSED — Phase 2 is unblocked** (bw-board 97f9448): labwired ships a real crates/wasm; it builds from our fork with the pinned toolchain unchanged; 92.3 MB raw of which 71.2 MB DWARF; stripped 19.55 MB; **2.12 MB brotli** — smaller than lite's vendor chunk. Left before F103 ships on it: the wasm-bindgen surface → boundary-A adapter, and board-manifest → netlist bridge. (c) **AN3155 flasher landed** (bw-board 4e8e71c, src/stm32-isp.js): the ROM-bootloader USART protocol, clean-room from ST's app note, transport-agnostic (Web Serial adapts in a dozen lines), byte-exact mock-ROM tests 5/5 — init/GET_ID/extended-global-erase/chunked-write/GO, NACKs named. SILICON RUN STILL OWED, header says so; the port-picker UI is the known remaining piece (same gap the 'Live board' refusal already names). (d) **F030 serial console input** (lite 76c16e98a): the send box reaches USART1 RXNE/RDR through feedSerial — the panel's existing gate just makes it appear. **THIRD WAVE (owner: emulator-preference / arbitrary-firmware / thumb-emu / patch-firmware-to-silicon / stcgal-independence):** (e) **Emulator preference persists per device** (lite 84260a8f2): the debug panel's engine picker IS the setting — a pick is remembered per device in localStorage and wins over the device map on reload. (f) **Arbitrary firmware runs** (same commit): a 📂 chip loads a .bin (Pico/STM32, real flash image, word-0 SRAM-SP sanity check) or .hex/.ihx (8051/AVR) and Run executes THOSE bytes instead of the blocks — no symbols, so glow/yield/variables honestly vanish; run/pause/step-insn/pins/board/serial all work. This is also the answer to 'load arbitrary firmware': yes, now, for every emulated family. (g) **Thumb/ARM emulation beyond the F0 tier = the labwired-wasm path**, and its gate is already PASSED (2.12 MB brotli, STM32-PATH 97f9448): M3/M4/M7 + RISC-V + Xtensa arrive in one move when the wasm-bindgen→boundary-A bridge lands. The hand-rolled CortexM0Machine stays the light tier. (h) **Patch-firmware-to-silicon / stcgal-independence — DONE for STM32, already DONE for STC**: the fleet's Rust `stcbsl` crate (stc repo, silicon-verified STC89 flasher, MIT, written without needing stcgal) grew an STM32 arm — `an3155.rs` + the `stm32bsl` binary (stc main 9d9e781): the ROM bootloader over the SAME Wire/baud/IOSSIOSPEED layer, 8E1 parity, byte-for-byte twin of bw-board's JS src/stm32-isp.js, mock-ROM-tested. So NEITHER family needs stcgal: STC has stcbsl (silicon-proven), STM32 has stm32bsl + the browser Web-Serial path (src/stm32-isp.js). NOT yet wired: neither flasher is in a GUI/Tauri app (stcbsl is CLI-only, stm32bsl new; the browser JS twin has no port-picker UI) — that wiring is the remaining flashing task, alongside both families' first-silicon runs. **FOURTH WAVE (owner: 'flashing in CLI and GUI for every chip'):** the assessment landed as working code. (i) **GUI flashing for every serial-bootloader family** (lite 232014f78): a ⚡ Flash-to-board button in the IDE — STC (ISP), Arduino/ATmega328/168 (STK500v1), STM32F030 (AN3155) — compiles via the hosted service, opens Web Serial with the right line settings (8E1 for STM32, cold-boot prompt for STC), calls the vendored flasher. The flasher is stc-compiler's flash.js, now with STM32 added (stc-compiler 41a2bc0, 27/27 mock-ROM tests), VENDORED into lite as flasher.js (scripts/sync-flasher.mjs, pinned). Devices with no serial bootloader (ATtiny=ISP/SPI, 6502/Z80=EEPROM burner) get the image downloaded + the programmer named; the button hides. Browser-probed: shows for STC12/STM32, hidden for ATtiny85/Pico. (j) **CLI flashing artifacts + honest tool hints** (sb3-creator f5e773d): `bw compile` now emits a real STM32 flash image (boot words verified) and AVR Intel HEX, each naming the right tool — stm32bsl / avrdude / (ATtiny) an ISP programmer — and the STC path no longer says 'for stcgal' (it names our own silicon-proven stcbsl + the cold-power-on step). Found+fixed a real codegen bug the branch exposed: `bw compile` called generateC without debug:true, and in that form an STM32 build LEAKS pico's BW_TIMER registers — the CLI now uses the proven form. (k) **The stcgal-independence table, complete**: STC=stcbsl (silicon-proven), STM32=stm32bsl + browser, both MIT, neither needs stcgal. THE ARCHITECTURE, stated: browser Web Serial (flash.js/flasher.js) is the GUI flasher for all serial families; the Rust stcbsl/stm32bsl are the silicon-grade CLI flashers; `bw compile` builds the artifact + names the tool. Remaining: oracle on VPS/cron (briefed), stc-compiler redeploy for 0dfc8d9+d028a14+41a2bc0 at the next cap window, FIRST SILICON RUNS (STM32 flash, re-confirm STC, and every browser flasher per BENCH-FLASHING.md — none has touched real hardware), Tauri native-serial flashing (so Safari-engine/Windows get it), ATmega2560 STK500v2, EXTI when a consumer exists. |
| measured thresholds: sb3-creator half | bw-audit | 2026-08-25 | The counterpart to the lite half I closed (`docs/WAIT-CENSUS.md`). Re-measured today, so the claim starts from a number rather than the doc's stale one: **sb3-creator 250 bounding literals, 38 evidenced, 212 NOT** — 203 floors, 18 timeout-ms, 11 ceilings, 7 tolerances, 4 size-caps, 3 pins, 2 concurrency, 2 timeout-min. (lite is 282/5/277, and its 119 timeout-ms are the part already done.) `docs/MEASURED-THRESHOLDS.md` says ~430 unprobed across both and prioritises by what an unevidenced number COSTS; that ordering stands and I am not re-litigating it. **What I intend to add is the lite lane's lesson:** there, the two findings that mattered needed no probing at all — 40 of 119 literals sat in `_tmp-` scratch files nothing runs, and the population the inventory does not collect (249 fixed sleeps against 119 bounds) was larger than the one it does. So: classify before probing, and look for the literals the instrument cannot see, rather than bisecting 212 flip points. Floors dominate here (203 of 250) and a floor's flip point IS cheap to bisect, which is the opposite of lite's browser waits — so the sb3-creator half should end with real probes where lite's ended with a census. **Classification pass DONE, branch `probe/thresholds-sb3` (`d7f2c3c`), written up in sb3-creator's `docs/MEASURED-THRESHOLDS.md`.** Result: **both lite findings failed to transfer, and that is the result.** No dead tier — 189 of the 212 unevidenced literals are in files `npm test` actually globs, no `_tmp-` equivalent to classify away. No large uncollected population either: I expected exact equality to hide a stricter class of bound (the inventory only sees `> >= < <=` and named option keys), and it does not — of 1,683 exact-equality sites, 562 are a vendored browser bundle and the rest are expected RESULTS (`assert.equal(run([…]).shown, 168)` is arithmetic) and fixture shapes (`rom.length === 0x8000` is the 28C256 the bench draws). So this half must be probed, as claimed. **The measurement that needed no probe:** a corpus floor's risk is its SLACK, and slack is directly countable — `circuitFiles.length >= 1034` against an actual **2099**, so **50.7 % of the circuit corpus could vanish before it fires**, and that literal is `docs/MEASURED-THRESHOLDS.md`'s own opening story (§1 calls 1034 'correct until a vendor added 58 circuits'; it was corrected then and never revisited). Four `> 1500` at ~28.5 %, four `> 900` at ~10.5 %; the healthy shape is `>= 819` at 5.9 % and `>= 2000` at 4.7 %. **PROBE HALF DONE (`42c6b24`) — rig built, 29/29 floors CAN FIRE.** Scratch rig at `/mnt/volume1/code/wt/rig-thresholds`: both siblings cloned from their local paths at the PINNED shas (`bw-board a301937`, `bw-circuit-ui af5cc08`), bw-board's deps from its own lockfile rather than symlinked to a live tree, 61 MB, **nobody else's checkout touched**. **A second rig axis I had not counted: the RUNTIME.** This box is node 20.20.2, CI pins 22, and two test files import `globSync` from `node:fs` (node 22 only) so they cannot be IMPORTED here — module-load failure, not an assertion — blocking 4 of 34 targets including the worst. **29/29 probed floors CAN FIRE: none is decoration**, which is a clean negative result and moves all the value into the slack. Three margins bisected, all three agreeing with my direct counts (kcl-residual green to 2098/red 2099 vs count 2099; flat-variants 1005/1006 vs 1006; expected-quantities 2446/2447 vs 2447) — two independent methods, same cross-check the lite half got. **One reconciliation:** the probe reports kcl-residual's headroom as 40 % and my table 28.5 %; the probe divides by the FLOOR, I divide by the CORPUS (how much can vanish before it fires). Both true, different questions, stated rather than silently rescaled. Superseded, kept for the record: ~~**BLOCKED for the probe half, on the box not the code:**~~ `threshold-probe.mjs` refuses to run because the sibling rig has drifted from `siblings.json`'s pins and bw-circuit-ui's tree is DIRTY — they are bw-cui2's and bw-lessons' live working copies, so a probe here would measure their work in progress. `--allow-rig-drift` exists and is the wrong answer. Unblock: clone both siblings at the pinned shas into a scratch rig and point `BW_BOARD`/`BW_CIRCUIT_UI` there — touches nobody else's checkout, and I will do that next unless someone objects. **Rule 5 — A THRESHOLD VALUE HAS NOW MOVED, as this row promised it would say.** `circuit-corpus-contract`'s `circuitFiles.length >= 1034` is raised to **1970** in its own commit (sb3-creator `73d3174`, landed on main). 1034 was 49.3 % of an actual 2099, so 1065 circuits could disappear before it fired; 1970 is 93.9 %, which is inside the band its two NEIGHBOURS IN THE SAME ASSERTION already use (259/278 = 93.2 %, 819/870 = 94.1 %) — so the value is not invented, it makes three floors in one block consistent and 1034 was the outlier by a factor of two. **Verified under a rig this box does not have by default:** siblings at the pinned shas in a scratch clone, plus **node 22** — the file imports `globSync` from `node:fs`, so on this box's node 20 it cannot even be IMPORTED. A pinned `node-v22.11.0` was fetched to scratch with its SHA-256 checked against nodejs.org's `SHASUMS256.txt` first, because a toolchain fetched by name is the same defect as a corpus fetched by name. Under that rig: **green at 1970, green at 2099, RED at 2100** — the flip point is exactly the corpus size, so the gate confirms 2099 itself rather than only my glob. CI green on the branch (`32868190828`) before landing. The literal now reads `evidenced: true` in `threshold-inventory`, which is the campaign's actual unit of work rather than the number. **Footnote that strengthens the case:** between the probe and the landing the corpus grew to **2100**, so the old floor was drifting further out of date while the lane ran. Only ONE value moved; the other 28 probed floors are reported, not touched. **Still unprobeable here:** the 4 targets in the two `globSync` files — 3 remain, since this one is now done. **On run `32869829938` (my floor commit) coming back RED — checked before repairing, and the repair was not mine to make.** Four failures, and the floor caused none: `examples/71-calculator-pcb` and `test/stm32f0-chain.test.mjs` were BOTH already present at `b27625b`, the main I rebased onto. Three were the enrolment coverage fixture going stale against `71-calculator-pcb`, a NEW example somebody else added — the same commit that grew the corpus 2099→2100 while my lane ran, which I had already noticed and footnoted. **Those three are already fixed on main by `649fdda`**, which regenerated the fixture itself; I regenerated to check and the only diff was the `measuredOn` date stamp, so I discarded it rather than push a no-op. The fourth is a **sibling PIN LAG and it persists**: `test/stm32f0-chain.test.mjs` imports `siblings/bw-board/src/cortex-m0-machine.js`, which exists at bw-board `origin/master` (`6373ef6`) and NOT at the pinned `a301937`. `649fdda` added a second STM32 test with the same import, so current main is red on two. **Fixing it means bumping the bw-board pin — rule 5, and the STM32 author's lane, not mine to move under them.** Correcting myself once here too: I first reported `cortex-m0-machine.js` as never having existed, which was the stale-checkout trap — the LOCAL bw-board is at `182b26b` and lacks it; its remote has it. **Rule 5:** none expected — this lane reads and records; if it moves a threshold VALUE it will say so here before pushing. |
| gallery stc12 lag | RESOLVED — same lane as the DONE row `gallery stc12 catch-up` (bw-master), landed `7b68479`+`fc94e19` while this row was being written; nothing left to do | 2026-08-25 | Real upstream work, recorded so it does not rot. `CrispStrobe/extensions` has **12** stc12 opcodes at its own `origin/main`; lite's bundled copy and sb3-creator's `reference/extensions/stc12.js` both have **30**. The 18 absent are the KEYPAD4X4 / SEVENSEG8 / MATRIX8X8 waves, never pushed to the gallery. Costs a permanently red `ctarget` gate locally (*stc12 blocks agree across gallery, bundled, and sb3-creator*) — it does NOT fire in CI, which has no gallery checkout.  **NOT fully closed — measured after the catch-up landed, by bw-audit 2026-08-25.** The membership half IS fixed: gallery, lite's bundle and sb3-creator's reference now all carry **30/30/30** opcodes, set-identical, zero either-way difference. But `ctarget`'s *stc12 blocks agree across gallery, bundled, and sb3-creator* is **STILL RED**, and the defect has changed shape rather than gone: the two copies now differ in **ORDER**, not membership (`keypad` at gallery index 10 vs bundle 12, `whenkey` at 21 vs 13), and the comparison is `deepEqual` on arrays, which is order-sensitive. **The gate's own message hides this** — it says *opcodes must be identical between copies* and prints a one-sided diff, which reads exactly like missing opcodes; I spent a minute believing the catch-up had not landed. Block order is Scratch PALETTE order, so it is real rather than cosmetic, but whether the two copies must AGREE on it is a design call for whoever owns that relationship, not mine to settle in someone else's lane. Two honest options: align the bundle's order to the gallery's, or make the comparison about membership and say so in the message. **Not visible in CI either way** — CI has no gallery checkout, so this gate skips there and only ever fires on a developer box. bw-audit will take it if bw-master routes it here.|
| fleet coordination | bw-master (VPS) | 2026-08-25 | Successor to the Mac fleet-steer, brickwright-lite-61 and the A2-calculator sessions (all three closed/crashed 2026-08-25); running in screen `bw-master` on the VPS, brief in `/mnt/volume1/code/bw-master-hq/CLAUDE.md`. Fleet pass 09:13 UTC: all four VPS agents ALIVE and idle, each holding a typed-but-unsent next step — bw-lessons "take D7 next", bw-cui2 "fix the 105 pin-name collisions and the 172 label texts" (412k tokens, near saturation), bw-bundle "bump lite's vendored sb3-creator pin to 96b53b0" (360k, same), bw-audit empty prompt. Deploy VERIFIED caught up: deployed `gui.8807fe36.js` carries `_serialStamp`, so deploy-daily's 04:23 refresh (`d242ba588`) landed the perf wave. The `instruments report numbers` claim below shows pushes through 00:29 UTC today (D3 closed `89b59828a`, then D4, D8, corpus floor) — NOT reclaimable per protocol; left with its owner. Box hazard, measured: /mnt/volume1 at 99 % (3.4 G free), load ~14, swap 7 G deep from root's builds — the exact state in which the load-sensitive ceilings (bw-board setPin/ladder, lite lesson-numeric-contract) fire falsely, so heavy suites are HELD until it drains. **A2 rescue triage decided, none left to rot:** stc-compiler LANDED `a09c081` (warning rewording, py_compile checked), bw-board LANDED `ad1ae4e` (XPT2046 `vbatDivider===false` learning-board opt-out + corrected LEDBANK8 comment; board-ics 12/12 + a2-displays green, mna.js untouched), stc LANDED `f875c1c` (A2 bench measurements + examples 22–26), sb3-creator DELEGATED to bw-audit (its claim above), bw-circuit-ui HELD BY ME for the load drain — reviewed: measured-copper PRECHIN-A2 preset + ledbank8/sevenseg8 sidecars + gen-board-presets, base 32 behind but only `package.json` + `parts-data/index.js` overlap; will land from a worktree with the 21-scenario browser gate run before push. **11:30 UTC — owner steer applied (`/tmp/steer1.md`):** (1) DISK: deleted `/mnt/volume1/kero1-target` (~23 G cargo cache) under the owner's 2026-08-24 disk ruling ("per-checkout cargo target/ dirs are expendable caches… below ~15 GB free, clean before you build") — verified first: no cargo/rustc referenced it, `fuser` showed zero open files, kero1 session idle; keromaster's session (kerotakis-3b) was messaged before deletion; kero-basic-target (in active use) and the 29 G kerotakis DATA untouched. Disk 100 %→86 % (24 G free). kero1's next build recompiles from scratch — expected, this row is the notice. (2) bw-board's env gap CLOSED: the repo TRACKED a node_modules symlink the `node_modules/` ignore rule (trailing slash) couldn't see — untracked at `de7c51e`, real deps installed on this box; bw-audit's 48 cascade reds should evaporate. (3) bw-audit UNBLOCKED per the steer's ruling — on an already-red base the green-suite rule means NO NEW FAILING NAMES; it re-runs and lands `bdbe0c5`. (4) Owner: verify via GitHub CI henceforth, not local full-suite preflights. (5) PERF PRIORITY: owner reports the calculator sim still slow; the Mac session is profiling the run loop and holds that lane — perf-adjacent lite work is high priority, and the worker-offload lane stays OFF the VPS. bw-cui2 dispatched on its V/W lane 11:28 UTC. bw-lessons (D7) and bw-bundle (pin bump) wait for bw-audit's landing — D7 is sb3-creator-repo work and the bump target becomes bw-audit's tip. **12:00 UTC round:** (a) the fifth A2 rescue is LANDED AND VERIFIED — bw-circuit-ui master `8c3de47` (measured-copper PRECHIN-A2 preset, ledbank8/sevenseg8 sidecars, test:a2 in test:all; focused 35/35, gallery corpus sweeps 38/0, gen-board-presets byte-identical, CI run 32842197702 GREEN). Rule 5: no pin moved — sb3-creator still pins cui `410f8ce`, lite's vendored cui moves only on the next sync. (b) **sb3-creator main has been CI-RED since 00:00 UTC and nobody was watching**: `expected-quantities-hold` → pc89-rl-step EXPECTED #62 claims 31.6086 mA at one time constant, the engine finds nearest 0.05 mA in ONE reachable state (the step likely never fires), unrecorded in the exceptions file — runs 32791862899 (pc89's own push) and 32841752069 (`ef7a3ce`, the pico idle-skip lane, which rode onto the pre-existing red; its own content is not implicated). pc89 was lessons-recheck's D8 companion; that session may be dead — bw-audit is offered the fix as first follow-up after its landing (message delivered), bw-lessons is the fallback. bw-audit also told to rebase onto `ef7a3ce`; its no-new-failing-names subset rule survives (the red is main's, by name). (c) For the pico lane (Mac): nothing the VPS fleet has in flight touches the run loop — my cui landing is preset/sidecar data, bw-cui2's V/W is schematic text detectors. (d) lessons-recheck's `instruments report numbers` claim looks finished (D3 closed `89b59828a`) but releasing it is the owner's move per the by-NAME rule — flagged, not touched. **12:15 UTC:** the pico release's [deploy] marker sat on a LANES-only diff, which the gate skips as non-web — both the Mac session and I saw it within minutes and BOTH pushed empty refresh commits (`67844146c` theirs, `156c7b88d` mine — I pulled their fix in and stacked mine without re-reading the log; the redundant one is empty and costs at most one duplicate Vercel evaluation of an identical tree, but it is the photograph-of-main lesson again, recorded rather than tidied). The deploy of the 110x wave is now marker-correct either way. Also NOTE for bw-bundle's eventual dispatch: the Mac's vendor `882e7c316` already moved lite's sb3-creator pin to `293d22e`, so bw-bundle's queued "bump to 96b53b0" is obsolete twice over — its brief will be rewritten fresh after bw-audit lands. **12:40 UTC, steer2 applied:** bw-board master is `b1cc57e` (avr8js SLEEP fast-forward, rebased onto `de7c51e`; suite 2676/0), sb3-creator gained `692ad55` (AVR tasks sleep; single-main AVR deliberately untouched), the Mac is vendoring both into lite next push with NO deploy today (AVR rides deploy-daily; the idle fix used today's explicit deploy). bw-audit is landing its rescue NOW — visible on main: `86cc6cb`+`abcadd8` hold 78-a2-calculator back (its display kind has no cui part) while keeping its behaviour test. The 8051 idle lane is claimed above and dispatched to bw-lessons. **13:05 UTC:** bw-audit's rescue IS on main (`b627b0d`, minus the doubled coverage number) and it took the pc89 offer — `95e8bc8` DECLINES claim #62 with a verdict (a tau-point on a transient curve is not an operating point the DC gate can measure); CI queued, watched. Its 78-a2-calculator hold-back (`86cc6cb`+`abcadd8`) is CORRECT at sb3's layer even though cui `8c3de47` ships the sevenseg8/ledbank8 sidecars: sb3 PINS cui at `410f8ce`, which predates them. **Unlocking 78 is therefore its own unclaimed lane — "sb3 cui pin bump 410f8ce -> >=8c3de47" — and rule 5 on the attiny88 row chains it to the 135-circuit re-seat with assert-physics + kcl-residual measured either side.** Also recorded: lite's vendored cui `model/exporters/` lacks `easyeda-schematic.js` (the native exporter never rode a vendor sync) — fold into the next cui vendor into lite. **14:30 UTC round, box health + stash triage:** (a) killed root's hung `stc12_trace` (owner-authorized sudo) — a ucsim-stc oracle run asked for 2 ms of 01-blink that burned 95 % of a core for 13 d 18 h, parent dead; SIGTERM sufficed, load fell 29->17; sweep found no other day-old CPU burners. (b) **All FIVE stashes on the lite checkout triaged and dropped, each with a verdict, none blind:** the display-input WIP (its OLED opcodes are in main's vendored surface; mono_lcd/rgb_light live in the landed widget layer + bw-board `23c03ce`), an EMPTY stash, the steering-widgets WIP (its 59-test file is byte-identical in main; gauge implemented), the microbitPlus WIP (108 `microbitplus_` hits in main's surface), and a build-mirror snapshot (regenerable). Diffs archived at `/mnt/volume1/code/bw-master-hq/archive/` for the paranoid. (c) The four stale REMOTE branches (`feat/steering-widgets`, `feat/display-input-blocks`, `feat/lego-hub-faces`, `feat/faceplate-lego-hub`) are likewise superseded by landed evolutions (hub gate lives as `scripts/verify-lego-hub-faces.mjs`; faceplate example family in overlay) — left on origin as history, deleting them is the owner's call. (d) Claimed the arduino-import fixtures lane (row above). |

## DONE — recently, so nobody redoes it

| lane | who | landed | what |
| --- | --- | --- | --- |
| measured thresholds: sb3-creator half — phase 1 | bw-audit | sb3-creator `c4f2b8e`+`57a2ac8`+`73d3174` (all on main); **GREEN on main in run `76624414d` / `32885266648`, which contains the floor commit** | Classification, probe of the 27 population floors at >=100, and one repair. **Both lite findings failed to transfer, and that is the result:** no dead tier (189 of 212 unevidenced literals are in files `npm test` globs; no `_tmp-` equivalent), and no large uncollected population (of 1,683 exact-equality sites, 562 are a vendored browser bundle and the rest are expected RESULTS and fixture shapes, not bounds). **29/29 probed floors CAN FIRE** — none is decoration, so all the value is in the slack. Three margins bisected, each agreeing with a direct count (2099 / 1006 / 2447): two independent methods. **One value moved, in its own commit:** `circuitFiles.length >= 1034` -> **1970**. 1034 was 49.3 % of an actual 2099 — half the corpus could vanish before it fired — and 1970 is 93.9 %, inside the band its two neighbours in the SAME assertion already use (93.2 %, 94.1 %), so it makes three floors consistent rather than inventing a policy. Verified green at 1970, green at 2099, RED at 2100: the flip point IS the corpus size. Now `evidenced: true` in the inventory. **Postscript, checked not assumed:** run `32869829938` on that commit came back red and NONE of it was the floor — three enrolment-fixture failures from `71-calculator-pcb`, a new example someone else added (already fixed by `649fdda`; I regenerated, found only a date-stamp diff, discarded rather than push a no-op), and an STM32 sibling PIN LAG that is the STM32 author's lane. `76624414d` then went green with my floor in it. |
| instruments report numbers | lessons-recheck (released by bw-master at day close — the session is gone, the work is not) | lite `89b59828a` (D3), then D4 `b66b3dc79`, D8 `4977c32dc`, corpus floor `7c47f4558` | The Bode sweep reports numbers; the four lessons re-worded; all landed and CI-green by 00:29 UTC. The claim sat open all day because its session died on the Mac. Released per rule 3 — an abandoned claim is worse than no claim — with full credit to its owner; nothing here was redone. Its D8 companion pc89 defect was separately closed by bw-audit (`95e8bc8`). |
| D37 interrupts bench | bw-lessons | sb3-creator `8ea99e0`, lite `694e978b4` | **DONE — D37 CLOSED the same day it was opened.** sb3-creator `8ea99e0`, lite `694e978b4` (**Build + vendor-freshness both green**). sb3-creator's CI shows ONE failing name, `STM32F030`, which is bw-engine's lane and fails identically on the base commit `c7edd87` — no new failing names from this work, and the `machine-roms-boot` gate is confirmed to have RUN and passed there rather than skipped. **MEASURING CORRECTED THIS DEFECT TWICE, and both corrections are in the ledger rather than quietly dropped.** (1) I wrote that the CPUs' interrupt pins were UNCONNECTED — wrong: all **7** CPU benches tie `/IRQ` and `/NMI` to **VCC**, held inactive, which is correct idle wiring. The true statement is that no interrupt-capable device OUTPUT drives any CPU interrupt input anywhere in the gallery. (2) I wrote that a ROM could not fix it and the emitter needed a Z80 ISR axis — also wrong: **`M6502Machine` polls every chip's `irqAsserted`**, so the simulator never needed the wire. What was missing was a PROGRAM. **And my first survey rig reported `0` CPU benches** — the rig, not the corpus: `Circuit.fromJSON` throws `engine not injected` and my silent `catch` swallowed it. Proving the instrument before believing it is the only reason any of this is right. **THE FIX:** `eater6502-bench` — whose `program.bw` was the one-line stub `DEVICE EATER6502` — now ships an interrupt program, and the lesson moved to it from `z80-bench`. Built to be CONTRASTED rather than watched: the foreground counts on port A in a 9-cycle `INC`/`JMP` loop while the VIA's free-running T1 interrupts every 4097 cycles and its handler counts on port B. Both visible in the debugger with no instrument the bench lacks. **MEASURED on the machine extracted from its own circuit.json: ISR period 4094 / 4097 / 4100, mean 4097; foreground 452.84 passes per interrupt against 455.22 uninterrupted.** The mean is the VIA's own (latch + 2). The SPREAD is the lesson's actual subject — the 6502 finishes the instruction in hand before it vectors, so latency is a DISTRIBUTION, and a bench with zero jitter would teach the opposite of the truth. The deficit is what the interrupt costs: entry 7 + INC 6 + LDA 4 + RTI 6 = 23 cycles predicted, 21.4 measured. The gate asserts all four as BEHAVIOUR, not as constants. **MUTATIONS:** four on the sb3-creator side (`CLI`→NOP, the IER write clearing instead of setting T1, the ISR dropping its T1 acknowledge, the IRQ vector aimed back at RESET) and three on the lite side, each confirmed to APPLY before its result was believed. **My own D7 sentinel caught the bench move** — it pinned the lesson to `z80-bench` and said a ROM could not help; that clause is now the D37-closed clause and pins the new bench the same way. The `z80-bench` test is kept, not deleted: its ROM/RAM ranges and BBC BASIC fallback are still true, so it was renamed to say what it holds. **A HAND-ASSEMBLY BUG I CAUGHT BY DISASSEMBLING THE EMITTED IMAGE** rather than re-reading my source: the foreground `JMP` first targeted `$8024`, the `CLI`, instead of `$8025`, the `INC` — harmless, but it makes the loop 11 cycles instead of 9, and this bench exists so that number can be reasoned about. **ALSO REPAIRED A RED ON MAIN:** the corpus coverage floor sat at 1093 when `71-calculator-pcb` arrived with someone's vendor, so `circuit-corpus-invariants` was failing 1094 !== 1093; another session fixed it concurrently and I took theirs on the rebase. **Vendor churn is worth recording:** main's sb3-creator pin moved three times during this lane (`8ea99e0` → `b27625b` → `649fdda`), so I abandoned rebasing and re-applied my five files onto current main deterministically. Ledger prose re-derived by its own gate: **58 of 90**. Suites: lite 1037/0 on this content before the churn, affected subset 327/0 after. Worktrees `/mnt/volume1/code/wt/sb3-d37`, `/mnt/volume1/code/wt/lite-d37`, `/mnt/volume1/code/wt/bwb-d37` DELETED at close. **SHARED-CHECKOUT HAZARD, hit firsthand:** this release row was written into the shared lite checkout while another session held `.git/index.lock`; I waited for the lock rather than removing it, and by the time it cleared their `git add -A` had already swept my edit into THEIR commit `34c86ecc5`. The content is correct and pushed — only the attribution is theirs. Recorded rather than rewritten, same as the hazard row above. |
| D7 machine ROMs | bw-lessons | sb3-creator `0a20e24`+`c3f25c1`+`fb1ae3a`+`2cb91f0`+`93838a2`, lite `60144c73e` | **DONE — D7 CLOSED, and D37 opened for the part a ROM cannot fix.** sb3-creator `0a20e24`, `c3f25c1`, `fb1ae3a`, `2cb91f0`, `93838a2` (all on main, **CI green at `2cb91f0` and `93838a2`**, and the base `a0a4f14` was green so the verdict is clean). lite `60144c73e` (**Build + vendor-freshness both green**). **All three benches boot the image they ship**, each on the machine extracted from its OWN circuit.json: `eater6502-blink` walks one bit at 99,940 cycles/lamp (799.5 ms); `eater6502-vdp-hello` puts HELLO on the TMS9918 at row 11 col 13, 763/768 cells cleared; **`z80-pd-bench` walks at 736,147 cycles/lamp (99.847 ms), sweep 798.8 ms** — the hand-computed T-state arithmetic in `build-machine-roms.mjs` and the core agree to the cycle. **PLAN offered two options; the answer was a third** — ship the SOURCE and its assembler, so `--check` refuses a hand-edited `.bin` instead of asking anyone to trust it. **THE BRIEF NAMED THE WRONG BENCH and that was the work.** Both Z80 examples asserted *"There is no DEVICE Z80 program axis in the transpiler yet"* — false since the Z80 core landed. The axis is narrow (`z80Hw()` knows only `OUT0-7`/`IN0-7`), so `z80-bench` (MC6850 ACIA, no latch) correctly stays program-less with its comment rewritten to the TRUE reason, and `z80-pd-bench` (74HC374 + 74HC244 + `led0..led7`) got the program. **THE REAL PRIZE:** the first Z80 program in the corpus exposed that `cToPseudocode` never learned the Z80 — emitted C read back as `DEVICE STC12C5A60S2`. Fixed both halves. **And the second half ESCAPED its first mutation, for a reason worth keeping: the degraded read-back is ITSELF a fixed point, so the corpus's `pseudocode→C→pseudocode converges` check stayed green while the program's meaning was gone. Convergence is not faithfulness.** The new gate asserts every `turn on`/`turn off` returns as itself. **MUTATION DISCIPLINE PAID TWICE:** two mutations that looked like escapes were NO-OPS (a pattern that never matched — lite's vendored index has different indentation than sb3-creator's) and were caught only by grepping the mutated source before believing the result. **LITE HALF:** `circuit-tab.jsx` fetches `ex.files.rom` and dispatches the same `bw-machine-media-load` the presets use; `debug-panel` applies pending media BEFORE `syncProjectTokens`, and that ORDER is now pinned by the sentinel because its own comment records the bug where an auto-run token built a second runner with no media and showed a black VDP while the real program ran unseen. New `test/example-rom-autoload.test.mjs` (5/0) EXTRACTS AND EXECUTES the shipped try/catch, catch included; 4 mutations bite including deleting the block. **The OPEN DEFECT sentinel was CONVERTED, not deleted** — it watched the 28c256's params, which is why a declared `files.rom` never tripped it; it now asserts what is fixed and keeps pinning what is not (the runner still skips the build and still boots empty on its own). **Two 6502 lessons re-copied EN+DE to content version 3** (their hints said the bench boots empty and to pick a preset "or the CPU sits on BRK at $0000 for ever"). My first scan for that claim MISSED the German because it says LEEREM in caps and my regex was case-sensitive — re-scanned case-insensitively. **`declared-pins-wired` learned one pad alias** (Z80 says `OUT0`, the '374's terminal is `q0`). Adding the '374 to `PAD_PROVIDERS` was tried FIRST and was WRONG — its `oeb` leg sits on the ground net so all eight LED cathodes read as "on pad oeb", the same false reading the `isRail` note already records. Only the NAME needed resolving, against that part's own terminals rather than the flat global name set. Both limits are written down, including the mutation that does NOT go red and why it should not. **I ALSO DROPPED a pin I had declared and never read** (`PIN sw0 = IN0 INPUT`) — the gate was right to flag it; a pin a program does not read is an unbacked claim, and it was fixed UPSTREAM in sb3-creator so the next vendor cannot overwrite it. **D37 (new, open, 1 lesson):** `machines-interrupts-performance` asks what an interrupt costs and **neither Z80 bench wires an interrupt source** — `z80-bench`'s MC6850 has a real `irqb` pin but ZERO wires touch it or the Z80's `intb`/`nmib` holes, and `z80-pd-bench` has no interrupt-capable part (`Latch374.irqAsserted` is hard `false`). A ROM cannot fix it: it needs a wire and a Z80 ISR axis in the emitter. Ledger prose re-derived by its own gate: **57 of 90**. **A VENDOR RACE, recorded:** another agent vendored sb3-creator `f53f576a` into lite while I worked, which pulled my `z80-pd-bench/program.bw` in WITHOUT my gate fix and left lite main RED on `every declared pin is wired to something` (`c14f7b1`). My push repaired it. Upstream-first is right, but an example landing before its gate does is a window worth knowing about. **Suites:** lite 1036/0 (1 opt-in skip), sb3-creator gallery 2492/0, machine-roms-boot 11/0, enrolment 5/0. Three sb3-creator files need `fs.globSync` = Node 22 and this box has 20, so they are CI-only here — CI ran them. Worktrees `/mnt/volume1/code/wt/sb3-d7` and `/mnt/volume1/code/wt/lite-d7` DELETED at close. |
| the gallery moved, two measured numbers did not | bw-bundle (VPS) | lite `da44ea10a` | **The part of the pin/gallery lane that was NOT duplicated.** I found lite's gallery 43 files behind lite's OWN `sb3-creator` pin — 78-a2-calculator still two `seven_seg_4` while the pinned `b27625b` already had the `sevenseg8` overhaul, plus 71-calculator-pcb, `eater6502-bench/rom.bin` and 36 EXPECTED.md provenance stamps — and synced both trees to `73d3174`. While I was testing, `c52c1c00e` landed the same job at `649fdda`. `649fdda` contains `73d3174`, so **I dropped my vendor commit rather than merging it** — re-landing it would have been noise. **What their commit did not carry:** two gates hold numbers measured over that gallery and neither test file was in it, so both were RED on main — `circuit-corpus-invariants` 1094 !== 1093 and `simulator-driver-controls-respond` 60 !== 43. Re-derived on the landed tree rather than bumped to silence a red. 1093 -> 1094 is **71-calculator-pcb** (exactly one `circuit.json`; `eater6502-bench/rom.bin` is the other new file and is not a circuit) — that gate's own comment requires the commit to name the example. 43 -> 60 needed reading rather than bumping: the population went 33 benches / 67 pins to 34 / 84 and **only the counterfactual moved** — armed-low still 22, armed-at-the-rail still 1, so no bench joined the dead set with the repair in place, and the sibling test that catches real regressions (dead pins not in `EXPECTED_DEAD`) passed untouched. All 17 new pins are ones the repair rescues, so growth there means the repair covers MORE, not that something broke. Header table re-derived with the new denominators instead of left describing 67 pins. **On the gate that should have caught the stale gallery:** not a hole. `sync-sb3creator.mjs` prints the warning itself ("shipping them apart is exactly what turned CI red twice on 2026-08-25") and `vendor-freshness` runs the check — the run for the offending pin bump `ba02583ef` was still QUEUED, so it had not had its chance. **CI VERDICT: GREEN** — https://github.com/CrispStrobe/brickwright-lite/actions/runs/32873336914 on `694e978b`, the first sha after mine, so it carries both re-derived gates: build, deploy and verify-gui all success. `vendor-freshness` on my own `da44ea10a` was green too (https://github.com/CrispStrobe/brickwright-lite/actions/runs/32873174863) — the gate that would have caught the stale gallery, confirming pin and trees now agree. My own sha's Build was cancelled by the next CODE push 100 seconds later, which is ordinary supersede and NOT the ledger storm: LANES pushes no longer start a build at all, and the queue sat at 2 while this ran. |
| lite vendored cui -> af5cc08 | bw-bundle (VPS) | lite `bad648e32` | Lite's vendored cui `eade8e3` -> **`af5cc08`**, the revision sb3-creator pins as of `818557c`, so the two now name the same commit and the divergence recorded twice today — first lite-ahead, then sb3-ahead — is closed. **Deliberate pinning, so `--allow-stale` was required and is the right call:** cui master had already moved to `18ec88d` while I was reading, and the target here is what sb3-creator pins, not whatever HEAD reached. Verified as a property of the result, not from the script's exit code: 596 upstream `src/` files under the sync's filter, 596 vendored, 0 missing / 0 extra / 0 differing; build.yml's overlay-vs-pin guard also reproduced by hand against the sha-addressed raw URL. **One file moved and it is the point** — `hooks/useCircuit.js`: `loadInferred` cleared parts and wires but not BREADBOARDS, so the previous circuit's board survived and its strips went on resolving nets naming parts that had just been deleted; `_syncNetlist` emitted *references unknown part*, the engine refused the netlist, and **loading an example left the simulation dead**. `handleClear` had always cleared breadboards; that path never did. Local: vendor-manifest-contract, fetch-pinning, schematic-projection, schematic-visual-baselines, circuit-rendering-regressions 25/25. CI both green — Build https://github.com/CrispStrobe/brickwright-lite/actions/runs/32866108705 (`build: success`, all browser gates; the run reads `queued` only because the Pages deploy job waits on its environment) and vendor-freshness `success`. No deploy marker; rides deploy-daily. **Still open, recorded not bundled:** lite's `sb3-creator` pin is `f53f576a`, one commit behind that repo's main after `818557c`. |
| sevenseg8 / sb3-creator cui pin + 78-a2-calculator | bw-bundle (VPS) | sb3-creator `818557c` | Pin `bw-circuit-ui@410f8ce` -> **`af5cc08`** (master tip, CI green) in `test/fixtures/siblings.json` AND `.github/workflows/ci.yml`, full 40-hex in both; 78-a2-calculator restored from `4ac3dc00a` with its behaviour test. **The row's diagnosis does not survive measurement, and the correction is the useful part.** It said `sevenseg8` "exists ONLY in bw-board", that cui's loader falls back to `['a','b']`, and that the engine therefore rejects 78 with *unknown terminal a/b*. Measured on 78's real circuit.json with bw-board at the pinned `a301937`: under the OLD pin `410f8ce` the display ALREADY resolves with all thirteen named terminals (gnd, seg_a..seg_g, seg_dp, sel_a..sel_c, vcc) and no 'a'/'b' anywhere — identical under `af5cc08`. cui's own PLAN.md says why: `terminalsForKind` calls `engineTerminals(kind)` FIRST, and bw-board has had `sevenseg8` since `7097ac3`, an ancestor of the `88e9668` sb3-creator carried before yesterday's bump. So the engine could always NAME those terminals and the fallback was never reached. What the sidecar supplies is POSITIONS (the canvas looks them up by name) plus the kind's registration — 410f8ce: 0 sidecar files / 1 src mention; af5cc08: 2 / 3. **The bump is load-bearing for RENDERING 78, not for solving it.** Same action, different reason; the row's reason would have sent the next reader into the wrong repo. **The attiny88 chain was measured and does NOT fire.** That row says a cui bump means the 135 attiny88 circuits "need re-seating"; cui PLAN.md says the re-seat wants an explicit go-ahead and must come AFTER the `pa0`->`gnd2` rename (blocked on bw-board AND bw-parts) so the corpus is regenerated once, and that shipped circuits keep their legs because a seated part's `leadMap` is authoritative. PLAN.md is right: all **135** attiny88-seating files (69 `circuit.attiny88.json`, 64 `circuit-flat.attiny88.json`, 2 others, 70 dirs — the row's 135 is correct, my first count of 69 read one filename pattern) infer a **byte-identical netlist signature** either side; assert-physics + kcl-residual read 365/322/0/43 either side; the EXPECTED census reads 2447/1235/1/1236 (50.5 %) either side. The re-seat stays blocked on the rename and this bump performs no part of it. **The trap that row flags is real and points the other way:** main's copy of `a2-calculator-behavior.test.mjs` is 107 lines and richer, the overhaul's 92 and simpler, which looks like main being ahead — but `ff6cff2` wrote both, `b627b0d` landed the overhaul, `86cc6cb` reverted the example and `abcadd8` reverted the test to match. main's is the ORIGINAL (keypad 1,2,3,A vs the overhaul's 3,7,B,F), so restoring the example alone pairs a new program with an old keymap. Six files move together, all from `4ac3dc00a`, whose 78 files are byte-identical to `b627b0d`'s. Enrolment fixture: **up to date** — 78 was never de-enrolled, so nothing to regenerate, checked rather than skipped. Suite 6765/6668/0; the one red before it was my own provenance gate correctly catching 31 pages naming a moved cui revision, restamped, every changed line the pin string and nothing else. CI https://github.com/CrispStrobe/sb3-creator/actions/runs/32863245077 — *Lint · test · build* and *Browser render tests (WebGL)* both **success**. **Rule 5:** the divergence I recorded yesterday has REVERSED — sb3-creator now pins `af5cc08`, lite vendors `eade8e3`, so **sb3-creator is 4 commits AHEAD of lite**, not behind. Still intent, not drift. |
| stm32 path: phase 0 | bw-engine (lite-61, Mac) | bw-board `STM32-PATH.md` + `src/cortex-m0-machine.js` (master) | The scoped, license-verified route to STM32 (and ATmega8A) — READ STM32-PATH.md BEFORE touching any phase. Phase 0 PROVEN with real firmware: a standalone ARMv6-M machine around rp2040js's core (whose entire SoC surface is 8 bus methods — verified by grep, then by a gcc-compiled -mcpu=cortex-m0 blink booting from its own vector table, taking a bus-mapped NVIC interrupt, and parking on WFI >90% of machine time with the idle wave's wake-horizon jump; 3/3 deterministic tests incl. the honesty ledger: unmapped accesses are recorded, never silent zeros). **UNCLAIMED next phases, effort-ordered:** Phase 1 STM32F0 board (port Renode's MIT peripheral models — RCC/GPIOv2/TIM/USART — legitimacy verified: Renode is MIT; skeletons from ST SVD fetched at dev time; sb3-creator DEVICE STM32F030 + stc-compiler -mcpu=cortex-m0 target; webpack alias for the core's deep import); Phase 2 F103 Blue Pill (GPIO v1 CRL/CRH is a DIFFERENT model; every surface labels the ARMv6-M-subset caveat); Phase 3 Renode differential oracle (VPS-shaped, Mono installs there — the ucsim pattern without the GPL wall); Phase 4 ARMv7-M core extension (LARGE, unlocks foreign F103 binaries); ATmega8A (small: avr-chips config, Timer0 has no compare — tick on Timer2, MCUCR sleep gate, -mmcu=atmega8 in the compile service). **Hard rule from the license table: nviennot/stm32-emulator and Qiling are GPL-on-unicorn — architecture ideas clean-room only, not one line of code.** |
| CI verdicts flow again (paths-ignore) | bw-bundle (VPS) | `34cf38b78` (build.yml) + `5bf3d14db` (vendor-freshness) | The cause behind the 12 cancelled main Builds: every LANES row started a full vendor/install/build/Playwright run, and `concurrency: pages-<ref>` collapsed them into one survivor. Fixed on the owner's instruction — **not** with sb3-creator's blanket `**.md`, which would have disabled real gates here: `examples/**/intro.md` is SHIPPED (webpack copies `examples/` into `build/`; ExamplesBrowser fetches it at runtime), `examples/**/EXPECTED.md` and `docs/**` are TEST INPUTS, and `THIRD-PARTY-NOTICES.md` is a licence artifact three tests `readFileSync` and assert on. The list is explicit and top-level only; all nine names measured at **0 actual file reads** across `test/` and `scripts/`, against 3 for THIRD-PARTY-NOTICES.md, which is the control and is left out. **`vendor-freshness.yml` given the same nine names in `5bf3d14db`** — it was the workflow actually starving (five consecutive runs `queued` and never started, and a gate that never starts is not a gate). Its header argues for running on every push and that argument survives: the push run is an INTEGRITY check against `vendor-pins.json` whose inputs are `vendor-pins.json`, `overlay/**` and `scripts/**` — no sync script references any of the nine — and `paths-ignore` narrows only the `push` trigger, so the nightly staleness alarm at 04:17 is untouched. Top-level only there too: `sync-examples.mjs --check` compares `overlay/scratch-gui/examples/**` file for file, and that tree carries its OWN `README.md`/`README.de.md` alongside `intro.md` and `EXPECTED.md`. **Proved on real pushes, in both directions and across the pair — and the middle two rows are why one change was not enough:** `34cf38b78` (build.yml) -> Build + vendor-freshness · `6eb3c4a0f` (ledger only, after build.yml alone) -> **vendor-freshness still ran** · `5bf3d14db` (vendor-freshness.yml) -> Build + vendor-freshness · `9a1499cd6` (ledger only, after both) -> **NONE**. The change itself is CI-green: https://github.com/CrispStrobe/brickwright-lite/actions/runs/32856897660. |
| bw-board CI has never been green | bw-master (VPS) | bw-board `182b26b`, run 32854184017 **GREEN — the first green CI run in the repo's history** | Two eras of red, both closed: installs died on the tracked node_modules symlink until `de7c51e`, then every run failed exactly one test — sparse-lu "reuse ladder speed ordering", refactor a STABLE 3.3-3.6x slower than full factor on runners. Diagnosed by elimination and reproduction, not guessing: identical numbers across three unrelated commits (not the idle wave), a relative ordering (not load flake), same commit on this VPS passes under Node 20 and INVERTS under Node 22 (CI's version), and warm=10 restores refactor 10x FASTER than factor. The benchmark was measuring V8 first-call tier-up, which changed between Node versions — so every timed path now warms 10 reps before measurement, margins UNTOUCHED (the instrument measures the claim it names; nothing loosened). Verified 3x under Node 20 + 3x under Node 22 on a load-10 box before push. **The verify-via-gh-CI rule now applies to bw-board like everywhere else; the standing exception in this lane's claim is lifted.** **17:25 UTC flag for the stm32 phase-1 lane (Mac):** sb3-creator main is TWO runs red — "STM32F030 grows the rest of its cap" and "STM32F030 joins the retarget family" both fail the corpus gates: `all 1170 benches: engine accepts, peripherals reachable from the MCU`, `bench invariants: every device bench, canonical loader`, `all 2133 circuits load, preserve data, and reload electrically` (test 688). Timing: both predate the attiny88 pa0->gnd2 rename landing, so this is the STM32 bench/circuit integration, not the rename. lite's vendor of this tree passed lite's own gates, so the break is in sb3's corpus surface. Flagged here per the steer-channel convention; bw-master will not touch the lane. **DAY CLOSE, 19:50 UTC — verified, not assumed:** every checkout and worktree on the box is clean and pushed (12 trees swept, 0 ahead, 0 dirty); the fleet's four agents released all their lanes and idle with empty prompts (D7+D37 moved to DONE with shas, stale watcher shells killed); the DEPLOYED Pages GUI serves the whole green day — probed live: examples index carries stm32f030 at every level (181 entries), 78-a2-calculator (unlocked), 71-calculator-pcb, eater6502-bench; 72-soldered-key is correctly absent because its sb3 run is red (the Mac's lane, ~1 h old, their pattern is fast self-repair). The Vercel mirror follows at deploy-daily 04:23 per the quota policy — one empty [deploy] commit syncs it sooner if the owner wants that. Open at close: stm32 phase 1 (Mac, active), measured-thresholds (bw-audit, park-or-release instructed), labwired evaluation (UNCLAIMED, engine domain, best for a fresh-context session), and the sevenseg8-armed attiny88 re-seat rides bw-cui2's landed pa0->gnd2 rename. |
| idle wave complete: all four cores sleep | bw-engine (lite-61) + bw-lessons | bw-board `b387a91`, sb3-creator `a0a4f14`, emu8051-stc `2f1855a`, lite this push | Every compiled target now parks instead of spinning, each measured: **ARM/pico** 110x press-to-pixel (WFE + emitted vector table, deployed); **AVR** sim/wall 5.5→85 (sleep_cpu + adapter SLEEP fast-forward with exact park/wake semantics — blinkenrocket was the canary that caught the fall-through cut); **8051** (bw-lessons: PCON.IDL in the emu8051 fork + adapter stops sub-slicing a parked core + `PCON |= 0x01` emission); **6502** (WAI: native vectorless wake — T1 armed in IER, I stays set, machine jumps a parked core to its wake horizon with a veto for horizon-less chips; parking measured at exactly 1 ms cadence, 1983/2000). **Honesty:** the 6502's SIM win at the Eater's 1 MHz is negligible — the clock is too slow for the spin to have cost much; its value is contract parity, real-silicon power, and the machine fast-forward for higher-clocked configs. Sign-off traps recorded for whoever touches these: cc65 inline asm knows neither `wai` nor `.byte` (the opcode ships as code bytes in RODATA=ROM, called through a cast); a cc65 pass costs ~0.15 ms so the 6502 parks after ONE quiet pass where ARM/AVR wait for two; a `-t none` cl65 image carries NO reset vector (patch $FFFC like debug-parity does, or the core executes RAM zeros and every number you measure is about the wrong program). lite pins all three and rides deploy-daily. |
| idle wave complete: all five cores sleep | bw-engine (lite-61) + bw-lessons | bw-board `b387a91`, sb3-creator `a0a4f14`, emu8051-stc `2f1855a`, lite this push | Every compiled target now parks instead of spinning, each measured: **ARM/pico** 110x press-to-pixel (WFE + emitted vector table, deployed); **AVR** sim/wall 5.5→85 (sleep_cpu + adapter SLEEP fast-forward with exact park/wake semantics — blinkenrocket was the canary that caught the fall-through cut); **8051** (bw-lessons: PCON.IDL in the emu8051 fork + adapter stops sub-slicing a parked core + `PCON |= 0x01` emission); **6502** (WAI: native vectorless wake — T1 armed in IER, I stays set, machine jumps a parked core to its wake horizon with a veto for horizon-less chips; parking measured at exactly 1 ms cadence, 1983/2000). **z80 joined last** (bw-board `be4bb2d`): a halted Z80 burned 4 cycles per JS call — the ZX frame-HALT idiom crawled ~17,500 iterations per frame; the machine now jumps a halted CPU to the nearest INT-capable chip (ULA frame, IE-enabled CTC channels, TMS9918 VBLANK; AY/tilevga declare Infinity; unknown chips veto). Measured: 504 vs 874,424 machine steps per sim-second (1735x), foreign firmware (BBC BASIC, ZX games) benefits unchanged; DI+HALT deadlocks honestly but cheaply. The COMPILED z80 target's emission half (EI + IM1 handler + HALT in generated C, sdcc crt0 layout work) is a recorded follow-up, not landed — the machine-side jump covers the shipping z80 uses. **Honesty:** the 6502's SIM win at the Eater's 1 MHz is negligible — the clock is too slow for the spin to have cost much; its value is contract parity, real-silicon power, and the machine fast-forward for higher-clocked configs. Sign-off traps recorded for whoever touches these: cc65 inline asm knows neither `wai` nor `.byte` (the opcode ships as code bytes in RODATA=ROM, called through a cast); a cc65 pass costs ~0.15 ms so the 6502 parks after ONE quiet pass where ARM/AVR wait for two; a `-t none` cl65 image carries NO reset vector (patch $FFFC like debug-parity does, or the core executes RAM zeros and every number you measure is about the wrong program). lite pins all three and rides deploy-daily. |
| 8051 idle (PCON.IDL) | bw-lessons (dispatched by bw-master) | emu8051-stc `24177ae`+`e473e90`+`9d2f691`+`ecdd5fb`+`0e86a00`, bw-board `afcf85d`+`6a24f23`, sb3-creator `09c6753` | **All four brief steps done.** `core.c` always implemented PCON.IDL correctly — core stops, timers count, an enabled interrupt clears IDL and vectors — it just never got FASTER, because `stc12_advance_to` ground one oscillator clock either way. Measured first: idling was **1.37 sim/wall against 1.55 busy-spinning — slower than not sleeping**. Split: CPU tick 65 %, peripherals 35 %, so skipping only the CPU caps the win near 1.5x. **END TO END on real generated firmware** (sb3-creator → SDCC → emulator, 200 ms sim): 76-multimeter **563 ms → 14 ms**, 61-console-pong **259 ms → 19 ms**, ~96 % of clocks slept, and PC/time/all SFRs/all IRAM **identical both ways**. Adapter poll mode 830 ms → 2 ms, board advances 92 161 → 2. **The guard is narrow ON PURPOSE and this is the part to read before changing it:** the brief said "jump to the next timer event", and enumerating T0/T1/BRT/PCA/ADC/WDT is exactly how the avr8js cut broke blinkenrocket. So it asks ONE question — is Timer 0 the only thing that can happen next? — and returns 0 whenever anything else is armed or anything is uncertain. It also stops one counter tick SHORT of the overflow, so TF0, the PCA drive, the clock-out toggle and the vectoring all still run on the normal per-clock path. Widening it later is safe; narrowing it after a regression is not. **Canary:** 28 real hand-written stc images (incl. the A2 22–26 the brief named, built from source) — 0 divergent, `slept=0` on every one, i.e. provably inert on everything that exists today. **Oracles are differentials, never stopwatches** (no wall-clock budget anywhere, per the standing rule). They caught six things reading did not, incl. a DOUBLED T0 when `skip_timers` is false (34 ISR entries where arithmetic says 17 → the skip now refuses that config) and SBUF seeded by `rand()` (caught only because the INERT cases flagged it too). **Two of my own claims were nearly false:** the corpus canary's first run would have reported "12 images, 0 divergent" over twelve EMPTY emulators (`emu_load_hex` returns 0 on SUCCESS, misread as failure); and the bw-board gate was green locally / red in CI because the sibling sits outside the package locally and inside it in CI, where `"type":"module"` turns Emscripten's CJS into ESM — `createRequire` does NOT fix that, a `{"type":"commonjs"}` marker does, and the candidate order now tries the CI layout FIRST so a local run cannot keep passing on the easy path. **CI: pushed, runs still QUEUED at hand-off** (fleet backlog) — <https://github.com/CrispStrobe/bw-board/actions> and <https://github.com/CrispStrobe/sb3-creator/actions>; not claiming green. Local instead: bw-board **2685 tests, 2655 pass, 2 fail** — both wall-clock timing tests (`AC: honesty and speed`, `perf budget`) that fail MORE at `b1cc57e` before my work (8/2 vs 9/1), i.e. load-sensitive and not mine; sb3-creator ctarget 199/1, codegen 73/0, exec 37/0 (the 1 is the known gallery/bundled opcode drift, red on clean main); emu8051-stc native oracle + wasm 43/0 + canary green. **Pre-existing reds I did NOT touch:** bw-board `reuse ladder speed ordering` and sb3-creator `EXPECTED.md quantities hold against the engine`, both red before this lane. emu8051-stc has NO push CI (only a manual SDCC workflow), so there the oracles ARE the verification. **Rule 5 — lite is UNAFFECTED and needs no action:** `sync-emu8051-wasm.mjs` fetches by pinned sha AND content hash, and its `EXPECT` still names the pre-change build, so nothing reaches lite until someone bumps `PIN`+`EXPECT` together. For that bump: `PIN e473e906c1a8db9a50e5b70ef42171e79a0de372`, wasm `fb0d75be03bb14e9860fb9c53ab51f5e6f05fd39eba9fe63ee9472facd15f985`, js `25eab6e31a4b5ce528733162abd3f3601c3f3345b6da285dfb1ad381964a9054` (`build/` untouched by the three later commits; exports are a SUPERSET — all 75 kept, 4 added — so the crash-fix floor cannot break by removal). Spec note: emu8051-stc `spec-updates/pcon-idle-fastforward.md`. **D7 (machine ROMs, 3 lessons) stays queued behind this lane, unstarted.** |
| vendor bw-circuit-ui into lite | bw-bundle (VPS) | `98e8c4c25` — then SUPERSEDED 8 minutes later by `bb3513bc4` (eade8e3), which contains it | Vendored `29f6da6` -> **`ac49da0`** and verified as a PROPERTY of the result, not from the script exiting 0: 596 upstream `src/` files under the sync's filter, 596 vendored, **0 missing / 0 extra / 0 differing by content**. Re-ran the same check against `eade8e3` after the other lane landed — also 596/0/0/0, and `ac49da0` is an ancestor of `eade8e3`, so my vendor was carried forward rather than reverted. **Two of the three arrivals the brief asked for needed CORRECTING, not delivering, and that is the durable part of this row:** (1) `model/exporters/easyeda-schematic.js` was said never to have ridden a vendor sync — it was already present and byte-identical to ac49da0, with `ExportNetlistMenu.jsx:19` (static) and `BoardCanvas.jsx:2754` (dynamic) wired at the same line numbers as upstream; it rode the `29f6da6` sync, and there was nothing to wire. (2) `gallery/board-prechin-a2.json` CANNOT arrive through this script and should not: it sits at the cui repo ROOT, outside the `src/` tree the sync walks, and `git grep` over ac49da0 finds one reader for it — cui's own `test/a2-board-preset.test.mjs`. Nothing under `src/` reads `gallery/`, and lite's ExamplesBrowser sources from bw-cfront's `examples/index.json`. Vendoring it would ship a file with no consumer and no gate. Its two SIDECARS were the half that mattered and they are here. **For the `sevenseg8 has no bw-circuit-ui part` row below: its stated blocker is gone upstream.** That row says "Fix: a `sevenseg8` sidecar in bw-circuit-ui/src/parts-data" — `8c3de47` added it, and lite now carries it with 13 NAMED terminals (vcc, gnd, seg_a..seg_dp, sel_a..sel_c), registered in `parts-data/index.js`'s exported `SIDECARS`, so the `['a','b']` fallback that produced *unknown terminal "a"/"b"* no longer applies. That row's remaining work is unchanged in scope — sb3-creator pins cui at `410f8ce`, which predates the sidecar by 34 commits — but its prerequisite is satisfied. **Rule 5:** lite-vendored cui (`eade8e3`) and sb3-creator-pinned cui (`410f8ce`) now DELIBERATELY diverge, lite ahead; read it as intent, not drift. **CI VERDICT: GREEN**, all 27 steps including every browser gate — https://github.com/CrispStrobe/brickwright-lite/actions/runs/32852621714 on `75ccc105b` (contains `bb3513bc4` -> `98e8c4c25`). It took a branch to get one: main had 12 consecutive `cancelled` Builds and 0 verdicts from 12:00 to 13:20, so I pushed main's sha to `ci/verdict-cui-eade8e3` and dispatched there — a branch ref has its own concurrency group. The FIRST attempt (32849665268) was itself cancelled 3m25s in, at *Build the editor*, with steps 1-15 already green including the overlay-vs-pin guard and the unit tests; the second ran clean. So the branch trick beats the cancelling but not the capacity, and both facts are in `BLOCKED.md` with the one-line cause — `build.yml` had no `paths-ignore`, so every LANES row started a full vendor/install/build/Playwright run. **Since FIXED in `34cf38b78`/`5bf3d14db` (row above), so do not act on that clause: it describes the blackout, not today's state**. Local before that: vendor-manifest + fetch-pinning 13/13, the three gates the V-fix moves 12/12, `sync --check` clean. No deploy marker; rides deploy-daily. |
| arduino-import stale fixtures (22) | bw-master (VPS) | sb3-creator `3772971` | Adjudicated: the warnings are CORRECTLY gone — the A2 rescue taught `cToPseudocode` to CONVERT `setup()`/`loop()` (Blink now imports as `WHEN flag clicked` + `FOREVER`; Smoothing's setup array-init converts; tonePitchFollower's tone path was already in PARSE_WARN_EXPECTED), so the recordings follow the importer. **Correcting the record without blame:** the earlier "main fails these 22 identically, so not the rescue's" finding compared identical NAMES whose CAUSES differed — on pre-rescue main those 22 failed from the bw-board deps cascade, on the branch from real fixture drift; the deps fix unmasked it. The fixtures had NO generator (hand-recorded once, gitignored corpus): `scripts/update-arduino-import-fixtures.mjs` now exists (--check / write / --all), updates the deliberate 21-case subset, and this box's corpus is regenerated — local suite 150/150, drift 0. **The corpus is machine-local: the Mac (and any other corpus holder) runs the script once after pulling.** CI never saw these (no corpus on runners), so no CI delta. |
| pc89#62 claim verdict | bw-audit | sb3-creator `95e8bc8` (main) | bw-master's first-choice follow-up, taken and closed. It was main's ONLY red. **The hypothesis was a bench whose switch never steps; it is not that, and the difference is the point.** pc89-rl-step is a series RL — vsource, resistor, inductor, gnd, no switch — but adding one would not have helped. The engine solves DC operating points, an inductor is a short at DC, so the bench has exactly ONE reachable state and every current in it reads the final 50 mA; stepping a switch gives 0 mA and 50 mA, never the value between. The claim names 63.2 % of final at one time constant: a point on the transient curve at t = tau, which no DC solve can hold. **And the document is RIGHT** — 0.632 x 50 = 31.6 mA, and its own table gives 31.6086 measured against 31.6060 ideal. So: unverifiable BY THE INSTRUMENT, not contradicted — and `expected-claim-exceptions.json`'s own note reserves that file for claims the engine CONTRADICTS, with unverifiable ones declined by the adjudicator and counted in the skipped column. Declined there, per-CLAIM not per-bench (a `benchClass` decline would have bought this one claim by dropping pc89's 5 V rail, 50 mA final and tau = 100 us, which stay checkable), and named by class so any document naming an instant on an RC/RL curve is declined for the same stated reason. **Measured, both sides:** before `checked 1236 / skipped 1209 / mismatched 2`, after `1236 / 1210 / 1` — exactly one claim moved and `checked` did not fall. The gate's own coverage ratchet holds untouched (compared 1237, floor 1225); I did not lower it, since that floor exists to stop exactly this kind of change paying for itself with coverage. The one remaining mismatch is the recorded arduino-sk-p04 exception. **CI CLOSED, and it took a second repair — recorded because the row read as finished when it was not.** Run `32846550819` came back RED, and the red was NOT the claim census: **Unit + VM tests were GREEN, main's suite clean for the first time since 00:00**, and the failure was the NEXT step, *Conformance gate is mutation-proven* at **29/31**. The earlier runs had died before ever reaching it, so my decline is what exposed this. **bw-master's first hypothesis — that my decline was too broad, so a reinstated defect got declined instead of detected — is disproved:** neither escaped mutation touches claims. Both edit the downstream manifest's `expectedMissing` / `pendingFix`. **The cause was my own landing, one step removed:** the A2 rescue re-vendored the `lite-stc12` snapshot — exactly the outcome that machinery exists to drive towards, since the eight opcodes really arrived — so both fields emptied and the two mutations that EDIT them had nothing left to edit. `delete m.pendingFix` on a manifest with no `pendingFix` changes nothing (measured), so the gate was asked to go red over an unchanged tree and truthfully reported green about nothing. Not escapes: **no-ops**. Fixed in sb3-creator `ee6a7c3` — both mutations now CONSTRUCT their precondition, and the no-op guard is **generic**, because per-mutation guards cannot close the class (the file's header already promised a no-op check on *every* file mutation and it was true of eight of the ten that touch files). Proved by reverting one to its old form: the runner now stops with *was a NO-OP — it saved 1 file(s) and changed none of them*. My first guard threw ENOENT on the mutation whose subject is a DELETED snapshot — a guard breaking the thing it guards; a deletion counts as a change now. `docs/GATE-INVENTORY.md` said 27 mutations against the prover's 31, stale before this branch, corrected in the same commit per that page's own rule. **VERIFIED GREEN: run `32852271207` on `a0a4f142` (contains `ee6a7c3`) — all three jobs success, and the prover step checked BY NAME reports `31/31 mutations caught`**; a green run with that step SKIPPED would look identical from outside. One false red on the way: `https://crispstrobe.github.io/extensions/CrispStrobe/stc12live.js returned 503`, a transient Pages hiccup in a live-network assertion — re-run, not treated as a verdict. **Local limit, stated:** the prover cannot run clean on this box (the live gallery checkout makes `stc12-conformance` red before any mutation — the 12-vs-30 lag CI never sees); I reproduced CI's condition with `BW_GALLERY` pointed at a copy of the vendored snapshot, under which my two go RED and the two INVERTED sibling-absence mutations MISS — my harness, not the tree, and both were green in CI. |
| easyeda importer: slot-vs-number + near-miss nets | stc-b6 (Mac) | cui `eade8e3`, vendored into lite this push | Both defects FIXED, not documented, on a real repaired board. (1) `readComponents` took a pin's number from the P-record's field 3 — the internal SLOT — and now takes the DISPLAYED number from section 4 (slot as fallback), the number a footprint pad is keyed on. (2) **The one-pin-net warning idea recorded here was WRONG, and the refutation is the valuable part**: implemented and run over the existing fixtures it fires on rc-divider's own VCC (a supply rail legitimately reaching one pin) — count alone cannot distinguish the defect, and shipping it would have trained everyone to ignore the warning. The discriminating signal is GEOMETRIC: a wire corner within one grid step of a label anchor while resolving to a DIFFERENT net (the flag's glyph is drawn outward from its connection point; nothing legitimate ends there). Measured: broken real board near-miss=1 (the actual defect), repaired=0, fixture=1, rc-divider=0 with orphan-nets=1 correctly NOT warned. `easyEdaNearMisses` warns; `easyEdaOrphanNets` is exported as a FACT, not a verdict. `solveSheet` factored so the two readers cannot drift; the committed synthetic fixture (`test/fixtures/easyeda-pin-numbering.json`, partition written down BEFORE the importer ran) replaces the withdrawn private-design reference — the owner's board is referenced nowhere. cui suite 1005→1012, 0 fail. **Also found: cui's `npm test` enumerates files explicitly — an unregistered test file silently never runs.** **Rule 5:** lite's cui pin moves to `eade8e3` in the same push as this row. |
| run-start latency (compile cache) | bw-engine (lite-61, Mac) | lite `22f455370` | Run→live-session, measured cold **4211 ms → warm 44 ms (95.7x)** on the Pocket Calculator: successful compile responses live in a 6-entry localStorage LRU keyed by the FULL (code, target, format) tuple — exact match, because a hash key could collide and serve a WRONG image. Failures never cached, quota errors evict then give up, no-localStorage degrades to a no-op — the cache can never be the reason a Run fails. lite-only (overlay bw-debug/debug-runner.js); no pins moved. test/compile-cache.test.mjs 6/6, lite suite 1031/0. Rides deploy-daily. |
| sb3-creator A2 rescue | bw-audit | sb3-creator `abcadd8` (main) | Landed under the owner's no-NEW-failing-names rule, both name-lists recorded as required. **LOCAL, main `293d22e` vs branch, bw-board deps present:** 408 tests, 381 pass, 27 fail on BOTH sides; 29 distinct failing names each; `comm` both ways EMPTY. **CI:** baseline run `32844143893` (`692ad55`, not mine) failed with 2 names — *EXPECTED.md quantities hold against the engine* and *no claim the engine contradicts is unrecorded*, i.e. exactly the pc89#62 failure bw-master diagnosed. My run `32844815486` (`abcadd8`): the same 2, nothing new. **What the review caught, in order of cost.** (1) A DUPLICATE `simulator-driver-controls-respond` enrolment entry — main already had one; both survived the cherry-pick, so the coverage fixture recorded **66 where the truth is 33** (33 distinct examples, each listed twice). Removed; both predicates select the same 33 of 278, so it was behaviour-preserving. Kept main's predicate although the rescue's (parsed `project.stc.pins` vs regexing `program.bw`) is the better instrument — swapping what a gate covers deserves its own measurement, so it is recorded, not smuggled. (2) **78-a2-calculator is HELD BACK, not landed** — see its own lane row below. (3) Provenance of the re-vendored downstream snapshot verified on all three counts (commit is an ancestor of lite main, hash matches the file at that commit and on disk, and the eight formerly-`expectedMissing` opcodes really are present — my first grep said otherwise and was wrong about quoting). (4) 79's EXPECTED lost the false *the ISR-owned shadow byte is why the program is correct anyway* claim, matching stc-compiler `a09c081`. **A mistake of mine, recorded:** my first push (`86cc6cb`) added six CI failures because I held 78's EXAMPLE and left its rewritten TEST in place — half-migrated in exactly the way that commit claimed to avoid. Repaired in `abcadd8`. The process error is the general one: after the revert I re-ran the gates that had been FAILING, not the gates the REVERT could break. **Rule 5:** lite's pin-bump target is now sb3-creator `abcadd8`, not `96b53b0` — this moves `src/utils/sb3Creator.js` (new LCD1602 parallel-bus syntax, C codegen, round-trip) and adds three gallery examples, which `sync-sb3creator` and `sync-examples` both vendor. bw-master is dispatching bw-bundle. |
| gallery stc12 catch-up | bw-master (VPS) | extensions `7b68479`+`fc94e19` | The gallery's `stc12.js` sat at 12 opcodes while sb3-creator's reference and lite's bundle carried 30 — the KEYPAD4X4/SEVENSEG8/MATRIX8X8 waves were pushed everywhere except upstream (bw-audit's find, re-verified: purely additive, 359 reference-only lines, 0 gallery-only). Copied the reference over (`7b68479`), then prettier-formatted to gallery style (`fc94e19` — formatting only, still 30 opcodes, eslint clean; the first push went CI-red on exactly that check, my own miss, repaired within minutes). CI AND Deploy green on `fc94e19`, so the LIVE gallery now serves all 30. **Rule 5:** the gallery copy now byte-diverges from sb3-creator's reference by FORMATTING only — opcode surfaces identical, so the `ctarget` gate's gallery-lag failure should clear; bw-audit must `git -C /mnt/volume1/code/extensions pull` before its final gate run. Any future gate that BYTE-compares gallery vs reference will need a prettier-aware comparison. |
| pico idle-skip (real-time sim) | bw-engine (lite-61, revived on the Mac) | sb3-creator `ef7a3ce`+`293d22e`, lite `681fb1383` | The owner's "calculator still slow and laggy", closed with a measured **110x**: browser press-to-pixel with a held key **34,516 ms → 315 ms**, main thread **0.9% → 68.3% idle**, interpreter share 89% → 13.5%. Root cause: every generated ARM flavor busy-spun between events (the tasks pass loop, the single-main forever — WHICH THE CALCULATOR IS, `debug:true` alone makes it a tasks build — delay_ms, wait_until), and rp2040js ground the spin at ~248M instructions per 2 s of sim. Fix: `bw_idle()` = TIMER ALARM0 at the next ms edge + **WFE behind an emitted 48-slot vector table** whose every slot is the latch-clearing wake handler. **The first design (`ef7a3ce`, PRIMASK-masked WFI, no table) was measured BROKEN — rp2040js correctly wakes by VECTORING, so the core jumped through VTOR=0 into garbage, 6x worse — and replaced in `293d22e`; if you touch this, WFE + real table is load-bearing, not style.** Node oracle: 2.7M instr / 2 s sim, sim/wall 0.25 → 18-31. REAL-build chain test 11.2 s → 1.46 s, keys+OLED verified. sb3-creator suite 6427 pass, by-name failure delta vs main: EMPTY (main's pc89 red is pre-existing and routed by bw-master). **Rule 5:** sb3-creator main tip moved `4512354 → ef7a3ce → 293d22e` (bw-audit's `bdbe0c5` interleaves — whoever is behind rebases; the no-new-names rule holds on both sides); lite vendors `293d22e` at `681fb1383` and the pin bump carries the corpus lane's pc* EXPECTED updates; every generated ARM program now sleeps on real silicon too (standard low-power idiom). AVR (sleep) and 8051 (PCON idle) idles remain follow-up lanes. Scratch semantics note: a wait(0)/forever spin now runs ≤2 passes per ms — the reference JS/MicroPython schedulers sleep 1 ms after EVERY pass, so C was the outlier, not the other way around. |
| device KCL-visibility | bw-engine (lite-61) | bw-board `b7cf145` | `solveMNA` records every registered device's stamped companions per NR iteration and extraction derives each terminal's current from exactly those records, so all ~40 models became KCL-visible at once rather than hook by hook; `model.branchCurrents` (dc-motor only) stays a per-terminal override. `test/device-kcl-visibility.test.mjs` pins the census relay-coil case (25 mA where 0 A was read), sign agreement with a series resistor in the same loop, a five-terminal KCL residual ~0, and override survival. Suite 2692 green. **The corpus diff was delivered BEFORE any pin moved and is EXACTLY EMPTY** — 2394 claims, 1215 checked, zero adjudication or value changes across `46f78ef` -> `b7cf145`, because the recorder adds no stamps (solutions bit-identical) and no checked claim reads a device terminal current today. Purely additive: terminals that read 0 A now read truth. |
| sb3-creator sibling pin + EXPECTED claims | bw-corpus | sb3-creator `7af6b5c`+`96b53b0` (main `96b53b0`) | **bw-board pin 88e9668 -> a301937**, both `test/fixtures/siblings.json` and `.github/workflows/ci.yml`, full 40-hex. Measured either side of the bump ALONE: compared 1209 of 2358 -> 1211, mismatched 1 -> 1, and the two claims that move are the two the hand-over predicted (47-battery-led, pc32-pnp-high-side), both agreeing. It is NOT corpus-identical the way it was reported, but the cause is not the engine: **bw-board TRACKS `node_modules` as a symlink to an absolute macOS path**, so a Linux checkout has no avr8js and five gates go red. Node's resolver walks past a broken symlink, which is why CI survives it and a local sibling worktree does not — for whoever owns bw-board. **Both declines resolved, and one of them named the wrong invariant:** the transistor decline said the extraction "is not KCL-consistent", but measured on 88e9668 itself a device's own terminals summed to zero in 11 of 11 samples (worst 0.0000 %). KCL at the device always held; what failed was agreement with the NETWORK. The replacement is the assertion it should have been — `test/device-branch-agreement.test.mjs`, a device terminal against the branch it feeds, as a magnitude so no sign convention can make it pass: **20 / 7 / 4 / 3 of 27 nets disagree at 88e9668 / 20283ab / b5c02b1 / a301937**, so the decline died in THREE stages, not the two recorded (44-darlington's collector still disagreed with its buzzer at b5c02b1). Red at all three earlier revisions, green only here. The 3 that remain are all a `gnd` part reading 0 A — **that is the `device KCL-visibility` lane above**, and the gate asserts the shape so it cannot be confused for a transistor defect. **The sharper finding is about my own gate:** a claim checked against its OWN ARITHMETIC is not a claim checked. pc78-belastete-quelle read 12 of 12 compared, 0 mismatched, while its central number was wrong by 30 % — 9/412 really is 21.8 mA, and the bench draws 16.719. Arithmetic-checked claims now go to the engine too. **And the mutation proof found one more:** reinstating pc78's defect left the suite GREEN, because a net's throughput summed POSITIVE terminal currents while the engine's sign is per-PART — 12.0113 mA seen from both ends reported as 24.0226 mA of throughput, which 21.8 agreed with inside the 10 % band. Half the sum of the magnitudes fixes it, and removing that phantom surfaced **pc11-direct-parallel shipping a blue Vf 3.1 LED against an intro that says "both light up at the same brightness"**. Coverage 1209/2358 (51.3 %) -> 1224/2356 (52.0 %) on one tree, 1230/2392 (51.4 %) after merging the 43-rc-timing/pc50 lane's 36 new claims; both kept, because only the first pair is like-for-like. Suite 6505/6414/0, lint clean, prover 31/31, CI https://github.com/CrispStrobe/sb3-creator/actions/runs/32785240117. **Rule 5:** this moves what sb3-creator's cross-repo gates load, so lite's vendored `sb3-creator` pin wants `96b53b0`. **FOR THE NEXT PIN BUMP (from bw-engine, 2026-08-25 — the pin is at `a301937`; `b7cf145` is 3 commits ahead and NOT yet pinned):** (a) device terminal claims must assert against the RESISTOR convention -- positive = out of the part into the net, so into-the-part legs read NEGATIVE -- and SEMICONDUCTOR claims against the opposite one. **VERIFIED HERE 2026-08-25**, not taken on trust: bw-engine handed over the bench and it reproduces to four decimals on a CLEAN checkout of `b7cf145` -- VCC(5V)->R1(1k)->D1(led)->GND, one current everywhere, `R1.a = -2.9703 mA` and `D1.anode = +2.9703 mA`, the SAME physical situation (current entering the leg) with opposite stored signs. I had said the into-part side was not greppable; that was wrong -- it is stated per-rule (`// into anode`), just never as a policy. What WAS wrong is now fixed at bw-board `b4c7756`: the resistor block's `currents.set('a', -i); // into terminal a` stored out-of-part while its comment said into -- the ONLY place in `mna.js` naming a convention for a passive part, pointing the wrong way, which is the likeliest reason the split survived. Comments only, values identical before and after. (b) the recorded decline **"a gnd part reading 0 A" is NOT closed** -- gnd parts are rail constraints, not registered devices, and a net-KCL sum over `branchCurrents` is unreliable while the two conventions stand (out-of-part: resistor/button/buzzer/dc-motor + the generic derivation; into-part: led/diode/zener/BJT/FET/isource). Needs convention unification or a vcc-style vs-row mirror -- its own lane, unclaimed. |
| lessons post-repair re-check | lessons-recheck | lite `8e2225d30`, sb3-creator `0c4ba90` | **The premise was false, and that IS the finding.** Every wave result whose cause is now fixed had already been re-worded on 2026-08-24 by the sessions that fixed it; every checkpoint still carrying a workaround has a cause still open, each confirmed by a green sentinel rather than by reading. Table with denominators: `docs/POST-REPAIR-RECHECK.md`. **What it found instead: the calculator repair was half of one.** Its arming loop passes `false` as the pull's RAIL, and a quasi pin idles HIGH — so arming it low clamped the net to ~0 V and no 8051 button could move its own pin. Over every example shipping a program with an INPUT pin and a circuit with a control on that pin's net (33 benches, 67 pins): **43 dead unarmed → 22 armed low → 1 armed at the rail**. The 22 were the whole 8051 side, three of them Wave 5 benches. Its defect class had NO gate in lite; `test/simulator-driver-controls-respond.test.mjs` now closes that, mutation-proved three ways. Then **D10** (Bode bench 0.159 Hz → 159.155 Hz, a point a decade below from 629 s of simulated time to 0.63) and **D11** (`43-rc-timing` gains a DISCHARGE switch — deliberately not the charge switch PLAN proposed, which would have stopped D23 reproducing on the only bench that shows it). Ledger: 13 of 36 closed, Wave 2 3→2, Wave 6 10→8. lite **1015 tests, 1014 pass, 0 fail, 1 skipped** (frozen tree), CI green <https://github.com/CrispStrobe/brickwright-lite/actions/runs/32781134651>; sb3-creator CI green <https://github.com/CrispStrobe/sb3-creator/actions/runs/32780104571>. **Rule 5:** moved lite's vendored `sb3-creator` — the driver change lands in every generated simulator program, and `pc50`'s capacitors and `43-rc-timing`'s parts changed, so anything pinning their numbers moves too. **Two things went wrong and are written down rather than tidied:** (1) I pushed a red — D10's push left lite CI failing and a parallel session repaired it better than I then did (`e51bda00a`, kept); re-measuring THEIR restoration found `measurement-rc-cursors` claiming the capacitor falls to 0.45 V in a tenth of a second, when one discharge time constant leaves it at 1.8847 V — wrong by 1.4 V, fixed in both languages and pinned. (2) I twice mutated the worktree under a running suite (a vendor, then a rebase), each time producing a failure that looked real and was not. **A suite run owns the worktree until it exits.** |
| release 0.1.8 | coordinating session | `v0.1.8` @ `80cbd0c1b` | Tagged, built, uploaded and **submitted for external beta review** (both builds WAITING_FOR_REVIEW, in the External Testers group). Cutting it needed main's red cleared first: check C had no frequency dimension, so a CORRECT Bode lesson read as unmatched with an empty nearest-measured list. **What the release found:** `docs/app-store-metadata.md` holds the tester notes and a gate asserts they exist, and NOTHING EVER SENT THEM -- 0.1.7 and 0.1.8 both reached App Store Connect with `whatsNew` empty in both locales under a green check. `scripts/push-tester-notes.mjs` now sends them from mobile.yml on a tag, mutation-proved 3/3; 0.1.8's are attached and read back. **0.1.7 was never distributed at all** -- it is not in the External Testers group, so the external train had been stalled at 0.1.6 since 2026-08-21. **Trap for whoever checks this next:** a build's `/betaGroups` endpoint reports `-` even for builds that ARE in a group; ask the GROUP for its builds instead, or you will read a live release as undistributed. |
| run-mode performance | bw-engine (lite-61) | bw-board `46f78ef`, cui `b01b552`, lite this push | The owner's "slow and laggy" calculator, profiled → fixed → re-measured (production builds, live browser): press-to-pixel latency **2788 → ~1000 ms median**, emulator sim/wall share **0.048 → ~0.22**. The frame budget was going to React, not the emulator: (1) resolveSeatedParts minted fresh part objects per render, defeating all child memoization — now memoized on a VALUE stamp (identity keys would serve stale seats, edits mutate in place); BreadboardView React.memo'd (~2,270 static hole circles were re-diffed at 20 Hz); (2) `footprint={bbFootprint(bb)}` re-defeated the memo with a fresh object — caught by the second profile; (3) runDrc ran in an IIFE per render — hoisted to useMemo on rev + warnings content stamp; (4) useBoard renderState 20→10 Hz; (5) circuit-tab's debugState guard compared a fresh-mapped array by identity — ALWAYS unequal → whole-tab setState at 60 Hz; now content-stamped with the ms tick throttled to 4 Hz; (6) engine: readPin's O(nets×terminals) scan cached per netlist (~30 pins × 60 slices/s). **Follow-up recorded, not attempted:** the interpreter itself (~55 MIPS on the main thread) is now the ceiling; the next lever is a worker offload — an architecture change needing its own lane. Also: z80-pd-bench SVG baseline accepted after element-level inspection (the peer lane's proper key/switch symbols — deliberate). |
| lite fetch pinning | bw-audit | `799e9139` (branch `fix/lite-fetch-pinning`) | Handed over from the coordinating session. **Denominator: 65 sites in lite resolve an external name to content; 55 named a mutable object; now 1**, stated not hidden. The four findings — `sync-sb3creator`/`sync-examples` at `REF='main'` on a caching CDN, `vendor-forward` cloning with no ref, a SEVEN-character WASM pin, 48 (not 47) unpinned `uses:` across 6 workflows — plus four the sweep found next to them: `sync-bw-board` resolved the sha correctly and then PRINTED `@master` anyway and kept a `?? REF` fallback; CI's ancestry `FLOOR` was abbreviated too; and **`build.yml`'s staleness guard compared the overlay against upstream `master` through the caching CDN — the 2026-08-23 mechanism sitting inside the guard meant to catch it**, warning whenever upstream moved and silent when the cache lied. It now compares against the sha lite PINS (first CI run: 0 mismatches, both named). **Open and listed, not hidden:** the ~40 MB offline library pack is fetched by a movable release tag and extracted onto users' devices with no integrity check; digest measured (`ca484cfe…bba436`, 42,054,931 B) and the fix is a Rust change this branch could not build. Gate `test/fetch-pinning.test.mjs` is a CENSUS — the set of fetch sites must equal a declared table — mutation-proved 11/11 (`npm run prove:pins`, wired into CI). **It caught its own prover** the moment that file became tracked, which is the self-matching shape again; fixed by assembling URL literals rather than widening the exclusion. CI: build **green on `799e9139`** — run `32762635996`, 1009 tests, 1008 pass, 0 fail, 1 skipped. After rebasing onto `d35e36893` the build run (`32764694167`) is RED, and **not from this lane**: every step of it is green (overlay-vs-pin 0 mismatches, prover 11/11, my gate 12/12) and the one failure is `lesson-numeric-contract` — see the note under the lessons lane below. My diff touches no lesson or bench file. `docs/FETCH-PINNING.md`. **Rule 5:** no pin VALUE moved, but three things others may notice — 48 `uses:` are shas not tags; the four remote syncs now FAIL rather than falling back to a branch name when the commits API is unreachable; and the staleness guard no longer warns merely because an upstream moved. **For the measured-thresholds lane:** a datum, free. Two full `npm test` sweeps on a loaded box put `lesson-numeric-contract`'s 15 s budget over its ceiling of 4 (5 benches), and the timeout discriminator read it correctly as *COMPUTING, not starved* — 438.0 s CPU in 977.7 s wall, ratio 0.448 against a CPU-bound child's 0.611, load 29.5→12.7 on 22 node processes. The same commit on a quiet CI runner: 0 fail. So that ceiling is load-sensitive, not stale. | **Its D10 note is closed:** main's red WAS `lesson-numeric-contract`, and the cause was not the lesson text but check C itself — the pool had no frequency dimension, so a correct 159.155 Hz reported with an EMPTY nearest-measured list. Corner frequencies are derived now (lite `e51bda00a`); the hint separately stopped citing the superseded 100 uF bench.
| widget editor + example faceplates | bw-engine (lite-61) | bw-board `aa29f9a`, sb3-creator `e2f0017`, lite this push | The owner's Widgets report, all three items, each verified live in the browser. (1) PART BINDING: the picker read `runtime.stc.board` — a field NOTHING writes (the 48ae8aaf0 epoch fix re-read a nonexistent path forever); now `bwRunBoard || circuitBoard`, with the RUN board published by debug-runner so widget presses land where the program executes. AND part-bound DISPLAYS had no data path at all — bindPanelToBoard only pushed inputs; it now runs a display pump: oled widgets mirror an ssd1306's actual GDDRAM pixels (new panel.setOledPixels + canvas face), lcd/terminal mirror character-device text lines, change-hashed. Verified: widget key '5' pressed → emulated firmware → OLED widget draws 5. (2) INSPECTOR ✓/✕: OK closes; Cancel reverts config/layout/binding/name to an open-time snapshot (proven live: binding flipped to pin, ✕, reopened → part/oled1). (3) EXAMPLES SHIP FACEPLATES: general loadExample now applies files.controller (was program-only-path exclusive), auto-opens the pane+dock (19 widgets were mounting into display:none), and 70-calculator ships a 17-key + pixel-screen faceplate bound to its PARTS. Also: two sentinels from the examples vendor resolved per their instructions (43-rc-timing discharge switch — wave-2 doc/hint/test updated, one-switch benches now sequence-probed in bench-capabilities; numeric-contract learns derived RC corners ±1 decade and skips instrument-cost quotes). **Rule 5:** sb3-creator examples index gains files.controller for 70-calculator; bw-board grows setOledPixels + the board-binding display pump. |
| project save/load + import-export wiring | bw-engine (lite-61) | cui `dce2175`, vendored here | The owner's save/load audit, unit + LIVE browser tests both directions. (1) SAVE-WHAT-YOU-SEE: the .sb3 bundle read the debounced `bw-circuit-autosave`, which only updates on an EDIT — a loaded-but-untouched example saved the PREVIOUS bench (measured: screen showed the 27-part calculator, file carried the 4-part demo). A synchronous `bw-project-bundle-collect` event now flushes the LIVE circuit (vm.runtime.circuitModel), pending code-editor debounce, and — for the first time — the WIDGETS panel (its `bw-ctl-widget-*` allowlist matched keys nobody ever wrote; the panel serializes to `bw-ctl-widgets` via its own toJSON). (2) LOAD INTO OPEN TABS: `bw-project-bundle-loaded` was dispatched and nobody listened; circuit-tab + code editor now re-seed, verified live (Blink 6 parts → load → calculator 27/17 in the open tab, no reload). (3) The native EasyEDA export had a handleExport case NO button invoked — unreachable from the UI; button added. (4) EVERY import ended in a pageerror (React event pool nulls e.target after await; input captured before). Live E2E: save→clear→load round-trips all tabs byte-identical; SPICE/KiCad/EasyEDA-native all download and parse; auto-detect imports the native file back (24/17, named skips only). lite suite 997/0, cui 1233/0. |
| calculator sim + canvas UX | bw-engine (lite-61) | sb3-creator `0777a17`, bw-circuit-ui `66e045b`, vendored here | The owner's calculator screenshot, all four symptoms root-caused, each verified end-to-end in the browser. (1) Keys dead in sim, "9" typing itself: TWO defects — the JS simulator driver mapped board-class INPUT pins to the 8051 'quasi' pull-up and never configured read-only pins at all (nets floated ~2.1 V, every key read pressed from boot, firmware typed its first-declared key '9'; fixed in sb3-creator: per-pin pulls matching the MicroPython rule + arm-per-board-instance), AND a sim-mode press released itself after ~80 ms (pointer capture on the canvas container retargets boundary events → spurious mouseleave on the button div → onButtonUp; fixed in cui: release owned by captured pointer-up). A held '5' now draws 5 on the OLED through the compiled-C rp2040js path. (2) Flying voltage pills: labels lifted straight-midpoint −25%·len, matching neither jumper geometry; now anchored at the arclength midpoint of the renderer's own polyline. (3) `net-coalesced` raw rule id on canvas: now "⧉ nets merged" + full engine message as tooltip (the warning itself is transient and by design). (4) Unreadable wiring: predominantly the STALE Vercel deploy (rate-limited + gate unwired until dd0e9c56d) — current render is orderly. **Rule 5:** sb3-creator + cui pins move here; the sb3-creator driver change also lands in every generated JS/Py simulator program. |
| bw-board source honesty | bw-engine (lite-61) | bw-board `b5c02b1`, vendored here | bw-corpus's two engine findings closed: `vsource` honors `rInternal` (DC + swept AC; the UI resolves legacy `battery` there, so pc77–pc80 and the battery benches now show loaded terminal voltage); saturated PNP stamps its E-B junction again (pc32's 430 mA base was a base FLOATING in the solve — supply moves 2.772→3.202 mA); FET extraction reads the region map (triode = gOn·vds) and the PMOS into-drain sign is now physical. Oracles cross-check extractions against neighbor-resistor currents. bw-board suite 2683/0, spec-updates/source-and-transistor-current-honesty.md. **Rule 5:** bw-corpus's rInternal canary is BUILT to fire; messaged with the exact moved numbers. (The 0.1.6 version-pin red I hit mid-vendor was fixed better by the release lane in `541c5f818` — literal removed, tester-notes presence asserted instead; my bump was dropped in the rebase.) |
| provenance / pins | bw-audit | `3beb78d` (sb3-creator) | audited every place either repo asserts what it verified; 11 action tags and 2 dep ranges pinned, a Pages fetch made sha-addressed, and a gate of its own found vacuous and recorded. MERGED — suite 6496/6400/0, prover 31/31 |
| schematic viewer | bw-circuit-ui | `def49c7fb040ba9f5cc76a1f4e9fa36d16e06856` | Second audit pass, which audited the DETECTORS rather than the drawing on the grounds that ten classes reporting zero from detectors written by the same hand is an untested claim. Five more real classes, all now 0/2098 and mutation-proved: a net's conductor ENDING on another's (426 circuits / 3,461), a shared corner (85 / 218), collinear within 4px (426 / 1,807), a solver-connected terminal with NO drawn pin (365 / 763 — an ATtiny88 drawn with no power at all), a label leader touching foreign copper (43 / 86). Two of my own detectors were disproved rather than reported (one over-counted 14x by treating legal X-crossings as contact). **Class H had been aimed at exactly the collinear case and read 0** because it inspected only `w.trunk` wires, and the previous fix had just converted 799 circuits' trunks into `segments` detours — a fix that relocated a defect past its own gate. 21 reviewed SVG baselines. CI 32704664707. |
| attiny88 footprint | bw-circuit-ui | `d4224262f88fd616502fc573309cc7edd3093e8a` | Picks up where `eac7a1a`'s canon measurement left off. The PDIP-28 top row ran in the wrong column order and **there were TWO footprints carrying it**: `src/model/footprints.js` (14/14 columns wrong) and `src/parts-data/attiny88.json` (8/14, and mirrored against the built-in). `FOOTPRINTS` is a Proxy where the built-in WINS, so fixing only the sidecar — the obvious move — changes nothing; 54 sidecar footprints are shadowed and inert. New `test/footprint-chirality.test.js`: a DIP may be seated rotated 180° but never MIRRORED, and the cross-product sign is exactly that invariant. Survey with denominators: 78 built-ins, 25 multi-row, 14 of 17 comparable pairs mirrored (ratchet, may only shrink). **Also corrects the record on the withdrawn gate** — design 3 was called "provably useless" on a mutation of 74hc595's SIDECAR, which the Proxy never reads, so it was green because nothing moved. **NOT CLAIMED, DO NOT START WITHOUT READING PLAN.md:** pin 22 is still named `pa0` (the package has no PA0; it is a second GND) because bw-board owns terminal NAMES and the canvas looks positions up BY NAME — renaming here alone renders it at the part origin. Ordered: bw-board, then this repo and bw-parts, then re-seat. **Rule 5:** this moves attiny88 seating geometry (13 of 28 legs), so whenever sb3-creator bumps its pinned `bw-circuit-ui@410f8ce`, its 135 attiny88 circuits need `gen-device-benches` re-seating with assert-physics and kcl-residual measured either side. Nothing moves until that pin is bumped. |
| easyeda export review | bw-circuit-ui | `06ffb68cccacaa5256687b2f18a2009219ea9c0c` | Adversarial review of the native exporter for the session that wrote it (not my lane; findings theirs, fixed in `953dc53`/`3304bcc`). Read the exported dialect with an INDEPENDENT reader sharing no code with our importer, because exporter and importer are two halves of one understanding and a shared assumption is invisible to a round trip. Found a drawn short in 2 of 2,098 files: a bottom escape's horizontal leg walked into the lane band. **The honest correction, which I sent after they had adopted my framing:** their round trip was NOT blind to it (36 classes vs 37 on the broken build) — the DENOMINATOR was, because the sweep read bare `circuit.json` and the `circuit-flat.<target>.json` twins were outside it. Same hyphen-vs-`\.` gap that cost my own lane a re-run. The instrument is kept as a script and deliberately NOT wired into `npm test`. |
| provenance / pins | bw-audit | `3beb78d` (sb3-creator) | branch complete and MERGED to main by the coordinating session; suite 6496/6400/0, prover 31/31 |
| EXPECTED.md claims | bw-corpus | `3beb78d` (sb3-creator) | Merged to main by the coordinating session; suite 6496/6400/0, prover 31/31. Every numeric claim in every EXPECTED.md enumerated and adjudicated: compared 34/2356 (1.4 %) -> 1195/2356 (50.7 %), each remaining claim declined by a STATED reason rather than being absent (`docs/EXPECTED-CLAIM-CENSUS.md`). Repairs with a which-side-was-wrong verdict: pc07-pot-dimmer (bench miswired — two seat errors left the LED dark at every pot position — then the doc), pico01-blink (bench had no LED, no resistor and zero wires under a page tabulating its current), 13-sos-morse (duty cycle read a letter's duration as the message's lit time). One open defect recorded with a verdict in `test/fixtures/expected-claim-exceptions.json`. 27 pages now name the engine revision they were derived against. **Follow-on, measured not guessed:** bw-board `b5c02b1` fixes both engine gaps this lane reported (vsource now honours rInternal; stampPNP's missing base junction, the real cause of the 430 mA readback). Measured before any pin moved: compared 1195 -> 1197, mismatched 1 -> 1, nothing regresses, and five of the six rInternal benches AGREE with the new engine while **pc78-belastete-quelle is wrong** (claims 9/412 = 21.8 mA without subtracting the LED Vf its own previous line names; measured 16.719 mA). Those numbers travel in the rInternal canary's failure message, so whoever bumps the pin inherits them. Those five commits are on `fix/expected-quantities` (tip, not a sha — it rebases), awaiting a second merge, and they carry more than the hand-over: removing a decline of mine that described an engine from BEFORE the pinned one ("the BJT model has no saturation region" — false of 88e9668, which has `params.vceSat`, the region FSM and the stiff clamp) unblocked 13 silenced claims, 12 of which AGREE. The thirteenth resolved as a bench defect with the bw-board lane: 44-darlington-motor instantiated a single NPN at beta 100 while its own intro.md teaches a Darlington and its step 2 tells the reader to "replace the Darlington with a single transistor and observe" — the shipped bench was the lesson's control condition. Fixed to beta 1000; V_CE 0.2048 V and buzzer 4.795 V are now DERIVED rather than asserted. Corpus: compared 1195 -> 1209 of 2358 (51.3 %), mismatched back to 1, exceptions `max` 1 -> 2 -> 1 with both moves recorded rather than smoothed. **For whoever takes the pin bump:** the canary carries a three-pin measurement table showing the transistor-current decline became false in TWO stages — the collector-demand half at bw-board `20283ab` (lesson-defects lane) and the PNP floating-base half at `b5c02b1` — so delete it only once the pin is past BOTH; a pin landing between them leaves it half true. |
| schematic viewer, third pass | bw-circuit-ui | `be4f0e17c4d85f3097a680ea3841e58f04600072`, `ff2a3ab` | Third pass, and the first to ask what is in the drawing that NO detector reads. All sixteen earlier classes compute from `projection.wires`; a symbol's OWN copper — the strokes both renderers draw from `schematic-symbols.js` — is in none of them, so roughly a third of the ink was outside every denominator. **403 drawn pins across 109 of 2,098 circuits sat where their symbol's artwork does not reach**: `disp-sevenseg` draws a digit as a figure-8 with two whiskers at y=0 and lands EIGHT wires on eight points with no copper. `schematic-symbols.js` had already diagnosed this for ONE kind (optocoupler is deliberately undrawn for exactly it) and fourteen others shipped with it. Now `artReachesPins`: art is used only when it reaches every pin of THIS instance, else the labelled box; `slide_switch` and the four two-input gates gained EXACT anchors, `relay`/`lm358` deliberately did not, because an invented anchor is a fabricated drawing. 76 of 14,843 symbols trade art for the box; the 21 older baselines are byte-identical. Also S (7 → 0, a wire ending on a pot's unconnected lead), R and T measured clean, Q2 measured and DISPROVED. **Class I turns out to be largely SUBSUMED by this pass's fix** — reverting `segmentHitsForeignPin` alone now gives 18 / 32 instead of 801 / 1,852, because a pin sits at the end of a lead that is now an obstacle; named rather than enjoyed, since a fix quietly doing an older fix's job makes the older gate look stronger than it is. **Rule 5, for whoever bumps the cui pin:** this changes the DRAWING of `relay`, `rgb_led`, `seven_seg*`, `opamp`, `lm358`, `tilt_sensor`, `dip_switch` (76 instances) and adds `symbol.generic` to every projected symbol — any consumer calling `shapeFor()` on a projection symbol must read that flag. Plus the owner's EasyEDA finding: our NetSolver folds in a T because that is KiCad's rule; EasyEDA does not imply one, so a T with no `J` is a crossing there and a connection here. Warned, not acted on. 1 of 4 vendor fixtures (hand-verified), 0 of 2,098 round-tripped through our own exporter. Suite 995/978/0/17, CI 32763155938 green. **Follow-up `43e2171`, CI 32780588441 green — and it CORRECTS this row's own claim.** "Class I is largely subsumed" came from one revert (18 circuits where pass two saw 801). Both rules reverted in all four combinations: pin ON/lead OFF gives S 7, pin OFF/lead ON gives I 18, BOTH off gives I 801 / S 585. The two forbid nearly the same geometry from opposite sides — a pin sits at the END of its own lead — so each hides ~98% of the OTHER's corpus evidence, and neither may be judged by its own revert while the other stands. Neither is redundant: the pin rule catches a conductor crossing a pin PERPENDICULAR to its lead (a legal crossing, so class S declines it), the lead rule catches a conductor ending on a lead at a non-pin point (invisible to class I). `both off` = 801 / 1,850 also reproduces pass two's 801 / 1,852, which is the check that no third change was quietly repairing the routing. `scripts/rule-isolation.mjs`, not in `npm test`. The instrument was WRONG first: it restored source from a string read at start-up, an earlier run had been killed mid-measurement leaving the source reverted, so the rerun took that as baseline and shifted every cell — now refuses a dirty tree and restores via `git checkout`, mutation-proved. |
| easyeda round-trip + the 403 | bw-circuit-ui | `bf22e34`, `b336eb9` | **(1) The 403 was already a FIX, not a finding** — Q has been 0 / 2,098 since `be4f0e1`; re-verified at `43e2171` and again in CI. What was missing is the proof the DoD names, and it is now there: the old mutation slides a pin off its lead (tests the DETECTOR), the new one REINTRODUCES the defect by forcing a generic symbol back onto `seven_segment`'s two-lead artwork — the exact state `disp-sevenseg` shipped in — and tests the projection's RULING, which is what keeps the corpus clean. **(2) EasyEDA both directions.** `easyEdaPartition` (the TESTED oracle, not an ad-hoc walk) gained `{strict:true}`, applying EasyEDA's junction rule through the same `NetSolver` via a new opt-in `solve({foldTeeAt})` — KiCad's readers untouched. Table: our own exports of all 2,098 circuits give **11,683 nets under BOTH rules, 2,098/2,098 agree, 0 J-less T, 0 split, 0 orphan**; the 4 vendor fixtures give 3/4, with `easyeda-rc-divider` orphaning `P1/2` at the J-less T at (100,-240). **The net-count column the DoD asked for would have called that file clean** — 2 nets both ways, because the T's victim net keeps two other members — so the gate asserts on node-level classification (`net-split` / `node-orphan`) and that trap has its own test. Import warning now carries coordinates, joint kinds AND the cost (the same document read the other way). **Denominator bound: this box has 4 EasyEDA documents and no live corpus** — `scripts/easyeda-roundtrip.mjs` takes `EASYEDA_DIR=<dir>` and produces the same classified table over real schematics, refusing an empty directory rather than printing a clean-looking zero. **(3) What still no detector reads is NOT zero.** 57,672 text runs are drawn and no class had read one: U (geometry off the page) **0 / 2098**, V (two pin NAMES overlapping) **105**, W (a net label's TEXT on foreign copper) **172**, both ratcheted. Narrowed deliberately — raw there are 813 text overlaps and 1,525 texts on copper, but 708 + 1,353 of those are a part's own labels being untidy and misleading nobody; counting them would have made W 8x bigger and 8x less true. Both text counts depend on a glyph model (0.6 em advance) and roughly DOUBLE between 0.50 and 0.62, so the conservative floor is 55 and 124 rather than zero, and the gate asserts the constant so a change fails loudly. **Rule 5: none** — no exported behaviour moved; `solve()` gained an optional argument with the old default, and the only shipped change is a richer import warning string. Suite 1005/988/0/17, CI 32785286382 green. |
| schematic text collisions (V/W) | bw-cui2 | `ef7d924`, `ac49da0` | **V 105 → 0, W 172 → 8**, routing untouched (identical segment/trunk/detour counts either side). **V**: all 105 were three kinds (555 ×57, rgb_led ×28, relay ×20). Widening the box — the conventional answer — was NOT available: the worst pair (`trigger`+`discharge`) needs a 31.2px inset while the PINS sit at 30, so a box wide enough swallows its own pins and pushing pins out moves the band `COL_W`/`PIN_HALF`/`BAND` derive from. The NAMES shrink instead, per symbol, never below 4.5 (the 555 lands at 5.12, not the floor; symbols with no facing pair keep 6.5 and render byte-identically). Gated at ZERO, not ratcheted. **The audit needed correcting too**: its `textRuns` modelled pin names at a hard-coded 6.5, so after the fix it was measuring a drawing nobody renders. **W**: three causes, each measured — the text box was not a candidate (172→43), a label's text was not an obstacle to routes committed later (43→…), and the reachable strip was too short where a trunk ~17px out sits inside every short leader length (→8). The 8 remainders are NAMED: one directory (`arduino-04-read-ascii-string`, 4 MCU variants × 2 rail labels), a trunk occupying the entire strip the label can reach; the gate ratchets at 8 **and asserts they are all in that directory** so a new one cannot hide in the allowance. **15 of 32 baselines moved and all 15 are mine** — rendered with the change reverted against the current corpus, 0 of 32 differ, so the corpus move (2,098→2,099, incl. the baselined `78-a2-calculator/circuit.json`) changed no baselined drawing. Reviewed, not accepted: element counts unchanged in all three sampled, A/B still 0 across 2,099. **Rule 5: none** — nothing another repo's gates load has moved. **Not run-loop adjacent.** Rig: bw-circuit-ui `29f6da6`, bw-board `1dac64c`, sb3-creator `1d846130d` (2,099 files). CI 32843838543 green, 994/980/0/14. No overlap with the A2 landing `8c3de47`. |
| label side-flip (the last 8 W) | bw-cui2 | `a799945` | **W 8 → 0 across 2,099 — and the flip itself never lands.** Implemented exactly as recorded (reversed vector, plus the body-clearance test the primary direction never needed), then INSTRUMENTED rather than reasoned about: rejected at every length and every nudge. A pin sits 30px from its symbol centre and the body reaches 26, so a flipped leader crosses the body at every length but the shortest, and at the shortest the TEXT lands on the body. So the search goes ROUND the pin instead of through it — up and down were already in `labelPin`'s vectors table and clear the body by leaving the row. Census over every label: **22,354 on the pin's own side, 0 flipped, 8 perpendicular**; the gate asserts `flipped + perpendicular > 0` so a dead search stops producing evidence. **Class X (a label's text over a symbol BODY) was measured BEFORE writing any of it** — the escape from a wire is to move, and where a label can move to is over a chip, so grading a W fix on W alone rewards trading one defect for another: 8 before (char_lcd_i2c ×6, lm358 ×2), exactly 8 after, now a ratchet by kind. Routing untouched; 0 of 32 baselines differ with the change applied AND reverted. **The corpus moved TWICE mid-lane** (`1d846130d` → `09c6753`): drawn pins 35,222 → 35,238 is entirely upstream swapping the calculator's sevenseg8 for two seven_seg_4, confirmed by rendering that circuit from both revisions. **Rule 5: none.** CI 32848925816 green, 996/982/0/14. |
| tests that never run | bw-cui2 | `f1e0bf7`, `efbafb5`, `4ed6a35` | From stc-b6's side-note that `npm test` enumerates files explicitly. **17 files ran nowhere (87 tests, 5 FAILING), and three whole scripts — `test:boards`, `test:render`, `test:a2` — existed while CI invoked NONE of them**, so 10 more were gated on someone remembering. The five failures diagnosed, not deleted: two were the TEST wrong (byte-window scrapes of BoardCanvas — `+2000` against a prop at 2321, `+5000` against a case at 5394; such a window tests the file's LENGTH, now structural extents), two are playwright, and one is REAL and **bw-board's**: `extract6502Machine` reports via1 span 8192 / acia1 4096 read off the board's decode while the `EATER6502` preset omits span and `m6502-machine.js:184` defaults it to `regs`=16 — different emulated behaviour (Eater's VIA mirrors across $6000-$7FFF; a read of $6010 finds it on hardware and on the extracted config, not on the preset's). Recorded as an OPEN DEFECT sentinel with the fold-back instruction; **bw-board owns both sides and I may not edit a sibling.** **And a gate I had cited was never running**: `easyeda-export.test.js`'s 2,098-circuit export→re-import round trip, quoted as evidence in SCHEMATIC-AUDIT's fourth pass, was unregistered AND resolved its corpus from a `$HOME/...` path existing on no machine, so it skipped always. It now runs: `2099 files — 2097 full, 2 partial, 0 MISMATCHED`. The claim was true; the evidence was not there. **I also broke CI once and fixed it**: wiring `test:render` in went red on `browserType.launch` because two files in it launch a browser and my detector grepped IMPORT forms rather than what a file DOES — the property is `*.launch(`, and there is now an INVARIANT (nothing reachable from CI may launch a browser) rather than a list, mutation-proved. `test/test-registration.test.js` gates all of it. **Rule 5: `package.json` and `.github/workflows/ci.yml` changed** — CI gains three steps; anyone vendoring cui should expect a longer build. npm test 1,675→1,776 (+95 mine, +5 stc-b6's file, whose registration conflicted on the same line and is kept). CI 32854558234 green. `docs/TEST-REGISTRATION.md`. |
| browser suite failures | bw-cui2 | `af5cc08` | **`test:browser` had never been executed** (no `node_modules` in this checkout; the files were outside every script until the previous lane). Made runnable → 40 tests, **19 FAIL** — not the 5 the orphan census counted, which was only the two files it could attempt. **19 → 0 in four causes.** (1) 19 failures, ONE missing server: four files navigate to a hardcoded `localhost:3100` and start nothing, assuming a human has `npm run dev` open; now `test/_dev-server.js`. While writing it, found `serial-console` and `pendant-attiny88` both claiming port 3195 — `node --test` is concurrent and vite uses `--strictPort`, so the loser dies; one port table, uniqueness asserted, **checked in CI where the browser suite never runs**. (2) The UI moved: icon buttons with titles, Save in the ⋯ menu, palette search + value-labelled parts, meter behind a toggle, SNAPSHOT/HARDWARE as a collapsed chip's `title` — every assertion repointed at the surface the UI uses, none deleted. (3) The harness had lost its numbered presets to the curriculum `examples` prop; `?examples=none` documented as the way back. (4) **A REAL PRODUCT DEFECT, which is what these tests existed to catch**: `useCircuit.loadInferred` cleared `parts` and `wires` but NOT `circuit.breadboards`, so the previous board survived and its strips resolved nets naming deleted parts → `Invalid netlist: references unknown part` × 46 → engine refuses everything → **load an example, simulation dead**. `handleClear` had always cleared them; one path missed it. One line. Fixed: 2.1 V junction and 14 % brightness now solve — the active-low comparison the simulator exists for. Mutation-proved both ways, and gated HEADLESSLY by `test/load-clears-breadboards.test.js` (incl. a test that the reproduction still reproduces) because the browser suite may never run in CI. Also noted: `debug-status.test.js`'s three tests **assert nothing at all** — they navigate and close, which is why they always 'passed'. **Rule 5: none.** test:browser 40/40, npm test 1,778/0, CI 32860618669 green. |
| debug-status: three vacuous tests | bw-cui2 | `ba56542` | One of the three navigated, waited 500 ms and closed **with no assertion at all**, under a comment saying what should have happened — and the comment was wrong too (`main.jsx` passes `haltReason: 'user'`, `bwMs` and tasks for snapshot, which it claimed were absent). **A vacuous test is worse than a missing one**: it occupies the slot the real check would go in and reports green from it. Why nobody wrote them: `DebugStatus` docks in the instruments panel, collapsed by default, so nothing it renders reaches `body.innerText`. And body text is not a usable handle — the simulation-controls panel renders the same ⏭/↩ glyphs, so a text assertion passes even in `live` mode where the component returns `null`; it now carries `data-debug-status` (the `data-meter-module` idiom) and every assertion is scoped to it. The tests turn on the ONE distinction the component exists for: `paused` and `snapshot` are both HALTED and differ only in the wall-time line (`+4.2 s ahead (board kept running)` present for snapshot, absent for paused, surface not mounted at all for live). A test that only checked "says HALTED" passes on either and cannot tell a pause from a snapshot — the falsehood the surface exists to prevent. **Mutation-proved three ways** (snapshot skew→0; paused given a 3 s skew; live given a debugState), each failing for its own reason with the other two green, so they are three independent checks and not one written thrice. **Rule 5: none.** npm test 1,781/0, test:browser 40/40, CI 32862802625 green. |
| sweep for tests that assert nothing | bw-cui2 | `18ec88d` | Swept all 138 test files. **The detector was wrong three times first, and that is the finding**: v1 said 29 (looked for the next `{`, so arrow EXPRESSION bodies `() => assert.ok(x)` were mis-parsed), v2 said 5 (the words "must sit BEFORE it (inside the svg)" in a COMMENT matched `it(`), v3 said 4 but accused ITSELF ("N test(s) contain no assertion" in its own error message matched `test(`). 29→5→4 — one mistake in three costumes, reading text that merely looks like code. Shipped detector blanks comments AND string contents to locate calls, reads names back from a copy that keeps strings, and carries six controls incl. four false-positive traps (helper delegation, arrow expr body, a message containing `test(s)`, prose containing `it (`). **The four fixed**: `interaction` + `presets` smoke tests (the throw was the only check and implicit — now explicit, with postconditions; a preset inferring ZERO parts used to sail through), and two `summary:` reporters that only console.logged — now ratchets at **33 losses / 11 files** (serialiser) and **162 naming diffs / 4 coverage gaps** (crosscheck), each failing in BOTH directions. Gated: `test/test-registration.test.js` asserts no test asserts nothing, **empty allowance list**, mutation-proved. **NOT MINE, flagged for the corpus-pin owner**: sb3-creator moved a THIRD time today (`09c6753`→`d7f2c3c`) and REVERTED 78-a2-calculator (`seven_seg_4`×2 → `sevenseg8`×1); its baseline is regenerated here (byte-identical to the render at `1d846130d`, reviewed 417→350 elements). That circuit has been swapped, swapped back and re-baselined three times in one day and **the baseline gate records no corpus sha**, so the churn is invisible until it fails — for whoever pushes next, not whoever moved it. **Rule 5: none.** npm test 1,782/0, CI 32864696201 green. |
| pin the corpus sha in the baseline gate | bw-cui2 | `f35ef8e`, `bbcdb3a` | A failing baseline now says **whose** change it was. `docs/schematic-baselines/CORPUS.json` is written by `render-schematic.mjs --baselines` **in the same act that writes the SVGs** (a stamp written separately drifts) and records a per-file CONTENT hash of all 32 baselined circuits plus the corpus git sha (informational). Hashes not just the sha: the sha says the tree moved, the hashes say whether it moved anything these baselines DRAW, and they work where the corpus is a copy/tarball/shallow clone. On a diff: source hash changed → `THE CORPUS MOVED … not a rendering regression`, naming file and both hashes; unchanged → `the drawing changed while every source circuit stayed byte-identical, so this IS a rendering change in this repo`. A move touching nothing baselined just prints a line and passes — which happened unprompted during verification (`42c6b24 -> 23a16d4`). **Mutation-proved 4 ways** (source changed upstream — made in a COPY via `EXAMPLES_DIR`, the read-only sibling never touched; projection changed with sources identical, `MARGIN_X` 70→72; a baseline added without re-stamping; the stamp deleted). Note: the first proof did NOT reproduce at first — moving a part's `x` changes nothing, the schematic computes its own layout — so the edit had to be something the drawing reads (`ohms`, drawn as a value label). **Then CI proved it live**: the corpus moved twice more mid-lane and `71-calculator-pcb` (2,099→2,100) turned the **Q2** ratchet red at `slide_switch 25 vs 24`. Raised to 25 only after checking the new instance part-by-part — `pwr`, `com`+`a`, second throw spare, `generic:false` — the same honest shape already recorded; Q2 is a census of honest drawings, not a defect count. Baselines, serialiser (33/11) and crosscheck (162/4) ratchets all held across the move. **Rule 5: none.** npm test 1,783/0, CI 32872562089 green. |
| attiny88 pa0 → gnd2 rename chain | bw-cui2 | bw-board `e1bda3f`, cui `be6361d`+`ff1dd15`, bw-parts `6eb8706` | The oldest ordered debt in my attiny88 row, landed in its recorded order (bw-board owns NAMES; the canvas looks POSITIONS up by name). PDIP-28 bonds out no port A — PA0-PA3 reach a pad only on the QFN-32 — so pin 22 is a SECOND GND (datasheet DS40002178). PA0-PA3 REMAIN in bw-board's `ATTINY88_PINS`: those are the DIE's registers, which exist whatever the package does. **The migration nobody had written**: a `leadMap` key is a terminal name, so a rename is a data migration there exactly as for a wire endpoint — and only the wire path had one. 70 of 135 attiny88 circuits seat the chip and all 70 name pin 22 in `seat.leadMap`; without it the hole stays occupied and the strip conducts while the leg belongs to a terminal the part no longer declares. It resolves against the **PACKAGE** terminal list, not the **DECLARED** one — a seated MCU commonly declares only the wired pin (`01-blink/circuit.attiny88.json` declares `["pb0"]` and seats 28 legs) and `resolveTerminal` only accepts an alias whose target is in the list it is given: the declared list migrated 50 of 70 and left 20 holding `pa0`, caught by a corpus sweep. **Electrically NEUTRAL, measured either side over all 135**: 135 solved, 0 refused, 2,430 nets, identical — ground comes from a `gnd` PART kind, not a terminal name, so the leg sits in the same hole, correctly named. **bw-parts was a REGRESSION TRAP**: cui's `src/parts-data/` is vendored from it and it still held the pre-audit attiny88 (9 coordinates + a sidecar footprint with rows swapped, all 28 leads), so `npm run sync:parts` would have undone `d422426`. Gated by `test/attiny88-pin22-gnd2.test.js`, mutation-proved twice, and it asserts **attiny2313 KEEPS its pa0** (that part really bonds one out). Also resolved a conflict with the Mac's `06c7ea7` STM32F030 part, whose own real `pa0` is untouched. **STEP 3 REMAINS, now a number not a debt**: re-seat is 70 of 70 seated circuits — **980 legs anchored on pin 1, 1,890 on the footprint's own reference**; both correct at their anchor, and the old "13 of 28" note was the pin-1 figure. `gen-device-benches seat` only seats UNSEATED benches, so those 70 need their seats dropped first — sb3-creator's corpus, per rule 5. **Rule 5: this ARMS the chain `818557c` measured as not-yet-firing**; sb3-creator's cui pin is `af5cc08` and whoever bumps it inherits the re-seat. CI green in bw-board (32877109539) and cui (32878967017); cui suite 1,787/1,783/0/4, bw-board 2,723/2,694/0/28 (its one failure, perf-budget setPin at 8,391 ops/sec, reproduces with the change STASHED at box load ~18 — a load-sensitive ceiling, not raised). |
| lessons | bw-lessons | `1b74412f8`… | Wave 4 and Wave 7 technical reviews; the two OPEN DEFECT sentinels resolved and replaced |
| wave open defects | lesson-defects | `9aa8008c7` | The 34 defects the seven waves left OPEN, as one table (`docs/WAVE-OPEN-DEFECTS.md`); ten closed, the rest in PLAN.md with what blocks each. **This is the other half of today's collision** — same two sentinels, from the open-defect side. Upstream: bw-board `20283ab`, bw-circuit-ui `8281919`, sb3-creator `65db1dd`+`42f1ff7`. **Moved the `sb3-creator` pin `1a83dfa`→`eac7a1a`** (rule 5: `sync-examples.mjs` now writes it — it never did, which is what turned a push red). |
| project save | coordinating session | `a69679979` | export errors caught and shown in the GUI; Circuit/Code/Widgets carried inside the `.sb3` as `brickwright/state.json` |
| footprints | coordinating session | `eac7a1a` (sb3-creator) | bw-parts vs bw-circuit-ui canon settled by measurement; attiny88 checked against the datasheet |
| CI | bw-audit | `965d172` (sb3-creator) | the abbreviated-pin blackout, the sibling lint, the timeout discriminator |

## A note from the second half of the collision

Recorded by the `lesson-defects` session, because the file above tells the story
from one side and the two sides differ in a way worth keeping.

I was not working from a stale worktree. I fetched, branched from `origin/main`,
and pushed within the hour. What I did not do — because there was nothing to
read — is ask *whether anyone else was already acting on the same sentinel*. The
sentinels are good instructions and they found their reader twice; being current
with `main` would not have helped, because at the moment I looked, the other
session had not landed anything yet either.

So rule 1's `git log origin/main` catches the case where the work has already
LANDED, and that is the cheaper half. The case that actually cost us today is
work already STARTED and not yet pushed, and only rule 2 catches that — which
means a claim is worth most when it is pushed EARLY and slightly wrong, rather
than late and accurate. Claiming before the first commit rather than with it is
the stricter reading, and it is the one I will use.

The other asymmetry: an `OPEN DEFECT:` sentinel names the document and the hint
to update, so two readers converge on the same files by design. That is the
sentinel working. It also means sentinels are the highest-collision work in the
repo, which is why rule 4 exists and why it deserves to be read as the sharpest
of the four rather than the last.

## A hazard this file's own protocol does not cover: the shared checkout

Recorded 2026-08-25 by bw-audit, from a live instance.

Several agents commit *in the same working copy* of this repo. Rules 1–6 are all
about racing on `main`; none of them covers racing on the WORKING TREE.

What happened: I edited `LANES.md`, then ran `git add LANES.md && git commit`.
Between those two, another session in the same checkout made its own commit and
swept my edit into it. My content reached `main` — intact, verified — under
somebody else's commit message, and `git log` shows no commit of mine at all.
The reflog is the only place the sequence survives.

Neither party did anything wrong and no rule was broken. But note the two ways
this bites:

- **Your reasoning is lost even though your text lands.** The row said what it
  needed to; the *why* had no commit to live in. If you care about the why, put
  it in the ROW, not only in the message.
- **The reverse is worse and has not happened yet:** their commit now contains a
  change they did not write and did not review. A half-finished edit of yours
  would ship the same way.

Cheapest mitigation, short of not sharing checkouts: keep the edit-to-commit
window to a single shell command, and after pushing, verify your text is in
`origin/main` rather than assuming your commit carried it — `git log` will not
tell you.

## What this does not solve

Two people can still claim the same thing within the same minute, and a claim in
a worktree nobody has pushed is invisible. This is a convention, not a lock. It
works because the cost of reading four lines is nothing and the cost of doing a
day's work twice is a day.
