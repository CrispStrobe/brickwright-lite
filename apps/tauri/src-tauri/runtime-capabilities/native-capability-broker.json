{
  "identifier": "native-capability-broker",
  "description": "Broker-webview half of the broker transport, plus the single semantic operation. Added at runtime only after the broker host acknowledges its immutable receiver. Disjoint from the main half: the editor can never reply, never open a session, and never reach a semantic operation.",
  "local": true,
  "windows": [],
  "webviews": [
    "capability-broker"
  ],
  "platforms": [
    "linux",
    "macOS",
    "windows"
  ],
  "permissions": [
    "allow-native-broker-invoke",
    "allow-native-broker-lease",
    "allow-native-broker-reply",
    "allow-native-broker-teardown"
  ]
}
