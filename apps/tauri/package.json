{
  "name": "brickwright-tauri",
  "version": "0.1.0",
  "private": true,
  "description": "Brickwright native app (Tauri 2) — one project targeting desktop (macOS/Windows/Linux) and mobile (iOS/Android). Wraps the brickwright-lite web build and provides a native ScratchLink (BLE + BTC/SPP).",
  "scripts": {
    "check:broker-assets": "cd ../.. && npm run package:broker-proof-pins:check && npm run package:broker-assets:check",
    "pretauri": "npm run check:broker-assets",
    "tauri": "tauri",
    "predev": "npm run check:broker-assets",
    "dev": "tauri dev",
    "prebuild": "npm run check:broker-assets",
    "build": "tauri build",
    "icon": "tauri icon ../../packages/scratch-gui/build/static/images/512.png",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev",
    "preios:device-build": "node scripts/prepare-ios-device-build.mjs",
    "ios:device-build": "tauri ios build --target aarch64 --export-method debugging --ci",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
