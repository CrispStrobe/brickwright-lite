// Import the editor directly so this bridge cannot pull the public index's
// reducer edge into the named editor stage.
import PaintEditor from 'scratch-paint/src/containers/paint-editor.jsx';

export default PaintEditor;
