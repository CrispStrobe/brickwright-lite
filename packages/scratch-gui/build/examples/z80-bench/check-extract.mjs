import{readFileSync as e}from"fs";import{dirname as o,join as t}from"path";import{fileURLToPath as r}from"url";import s from"node:assert/strict";const n=o(r(import.meta.url)),a=t(n,"circuit.json"),i=process.env.BW_BOARD_DIR||t(n,"..","..","..","..","bw-board"),{extractZ80Machine:c}=await import(t(i,"src","z80-extract.js")),l=c(JSON.parse(e(a,"utf8")));s.ok(l.ok,`extraction failed: ${l.reasons.join("; ")}`),s.deepEqual(l.reasons,[],"expected zero refusals");const p=["MAP RAM $8000-$FFFF","MAP ROM $0000-$7FFF","CHIP acia = MC6850 AT PORT $0080"];s.deepEqual(l.lines,p,`extraction lines do not match Searle preset:\n  got:  ${l.lines.join(", ")}\n  want: ${p.join(", ")}`),console.log("z80-bench: extraction matches Searle Z80 preset — PASS"),console.log("  lines:",l.lines),console.log("  notes:",l.notes);