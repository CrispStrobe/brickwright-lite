# Language x device matrix — one truth, shown to users, closed cell by cell

Status: **active plan, lane open**. Created 2026-09-05 by session `lego-ac`
(`Claude-Session: https://claude.ai/code/session_01L3xy8YZKHGfHAkysBEREXG`).
Owner of record for the truth lane (T1, T2) and the ASM reader (L1): lego-ac.
Every other task is unclaimed until a row in `LANES.md` says otherwise.

The product question this answers: *write in any language the Code tab offers,
run it on any chip the Circuit Designer or the device picker offers, in the
simulator and on real silicon.* Today that is partly true, partly false, and
nowhere written down in a form the GUI and the docs both read. This plan makes
one table the source of truth, shows it to the user, derives the docs from it,
and then lists what closes each cell that is still open.

---

## 1. Decisions (made — do not re-litigate in tasks)

### D1. A cell has two halves, and each half is a fact, not a verdict

For a language **L** and a device **D**, the cell records:

- **native** — L itself runs on D. The artefact is L's own program (a `.py`
  for MicroPython, a `.bas` typed into a BASIC ROM, a `.hex` from a C or ASM
  toolchain). Sub-facts: which toolchain, where it runs (`local` in the
  browser, `hosted` on stc-compiler, or `none` because the chip runs the
  language directly), and whether that artefact reaches the simulator, real
  silicon, or both.
- **lowered** — L is read into the dialect AST and re-emitted as something D
  runs natively. The learner's text does not run; its meaning does. Sub-facts:
  the reader used, the emitter used, and the native cell it lands in.

A cell is therefore one of **native**, **lowered**, or **both**. There is no
"refused" cell. Where a native half is physically impossible, the cell says
`native: null` **with a reason** (`ram`, `flash`, `no-port`, `no-emulator`,
`licence`) and the lowered half carries the load. "Physically impossible" is a
strong claim and every use of it in the table cites the number it rests on.

### D2. The table is data in the code, gated against the code

`overlay/scratch-gui/src/lib/bw-matrix/capabilities.js` is a frozen, versioned
data module. It is hand-authored — some facts (the hosted service's target
list) do not live in this repository — but every fact that *does* live here is
cross-checked by a test that reads the real source:

| fact in the table | checked against |
| --- | --- |
| device ids, groups, `compile`, `emulator` | `DEVICE_GROUPS` in `pseudocode-importer.jsx` |
| silicon transport per device | `flashFamily()` and the `flasher.js` exports |
| local vs hosted assembler | `LOCAL_ASM_TARGETS` in `bw-asm/assemble-route.js` |
| local vs hosted C compiler | `LOCAL_TARGETS` in `sdcc-wasm/compiler.js` and `smallerc-wasm/compiler.js` |
| which cores `generateC` emits for | the `_core` selection in `sb3-creator.js` |
| which languages have a reader | the `sb3-creator-*.js` reader modules |
| hosted compile/assemble targets | `docs/generated/hosted-targets.json`, a pinned snapshot (hand-read at stc-compiler `79df4b6d` on 2026-09-05; T2 adds the refresh script) |

A table row that the source contradicts fails the build by name. A table row
that nothing checks is marked `evidence: 'declared'` and rendered with that
label — the reader can see which facts are proven and which are only stated.

### D3. GUI and docs are both derived, never hand-maintained

Two GUI surfaces and one document read the same module:

- **Overall matrix** — a "What runs where?" view reachable from the Code tab's
  device dropdown: languages down, devices across, each cell native/lowered/
  both, with sim and silicon marks.
- **Contextual badge** — beside ▶ Run and ⬇ Deploy, for the *chosen* language
  and device: `native · sim + silicon`, `lowered via C · sim only`, or the
  reason nothing is available. The buttons' enabled state comes from the same
  lookup, so the badge cannot say one thing and the button do another.
- **`docs/generated/LANGUAGE-DEVICE-MATRIX.md`** — generated by
  `scripts/gen-language-device-matrix.mjs`, with `--check` wired into the build
  like the other generators. `docs/CHOOSING-HARDWARE.md` and
  `docs/device-matrix.md` link to it instead of carrying their own tables.

### D4. Native closures are preferred where a permissive runtime exists

Where a chip can run a language natively with a permissively licensed runtime,
we add the native half even though the lowered half already works, because a
learner who wrote Python and sees Python on the board learns something the
lowered path hides. Licences that qualify: MIT, BSD, Apache-2.0, zlib,
MPL-2.0 (file-level). GPL toolchains stay hosted, as they are today.

### D5. Peripheral verbs are a second table, joined to this one

Which parts a program can drive (`lcd print`, `oled pixel`, `set servo`) is
per part × per family, not per language × device, and is owned by the
part-profile lane (§3, Lane P). The matrix cell links to it: a lowered cell
that lands in C on the 8051 inherits the 8051 C driver set. The two tables
are joined by family id and nothing else.

---

## 2. The matrix as measured on 2026-09-05

Read before claiming a task; several cells were wrong in the folklore, and
**two of this plan's own gap claims were stale within the hour it was written**
(N2's assembler, N3's untried boot). Re-check a cell against the tree on the
day you act on it — a gap claim here has a shelf life measured in hours.

Legend: **N** native exists · **L** lowered exists · **n** / **l** closable,
task named · **—** physically out, reason given · `?` needs a measurement.

| language ↓ / device → | 8051 STC | AVR (Uno/Nano/Mega/ATtiny) | RP2040 Pico | STM32F030 | micro:bit / Calliope | 6502 Eater | Z80 bench | 8086 DOS bench | Arcade / PyBadge / SAMD51 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **Pseudocode** (the AST) | L via C | L via C | L via MicroPython, and via C for the emulator; the C artefact has no silicon deploy → **l** (L2) | L via C | L via MicroPython | L via C (cc65) and BASIC (`ms`) | L via BASIC (`bbc`); C emitted, no compiler → **l** (N1) | L via 8086 asm, in-browser, and via C (SmallerC, local) | L via PXT TypeScript, hosted; no UF2 → **l** (L4) |
| **Python** | L · native — (MicroPython needs ≥256 KB flash / 16 KB RAM; STC12 has 60 KB / 1.3 KB) | L · native — (ATmega328P 32 KB / 2 KB) | L · **N** MicroPython on silicon (REPL deploy) · sim **n** (N3) | L · native — (F030 16–32 KB flash) | L · **N** MicroPython, sim + silicon | L · native — (no port) | L · native — (no port) | L · native — (no port) | L · **N** CircuitPython on silicon (copy `code.py`) · sim — (no SAMD51 emulator, see CHOOSING-HARDWARE) |
| **JavaScript** | L · native — | L · native — | L · native **n** Kaluma (Apache-2.0) or Espruino (MPL-2.0), silicon (N5) | L · native — (Espruino needs F4-class) | L · native **n** PXT static TypeScript (MIT), the MakeCode path (N5) | L · native — | L · native — | L · native — | L via Arcade TS |
| **C** | **N** local SDCC-WASM + hosted · L | **N** hosted avr-gcc · L — picker `compile: true` (reconciled by T5, 2026-09-05) | **N** hosted bare-metal rp2040, runs in rp2040js · no UF2 deploy from the UI → (L2) | **N** hosted · L | **n** hosted has an `nrf52833` linker script and an ARM *assemble* chain, but no C compile target (N7) | **N** hosted cc65 · L | **n** SDCC `-mz80` hosted, not present (N1) | **N** local SmallerC-WASM + the assembler's NASM front end, sim only (N2 done 2026-09-05; `float`/`long` are named edges) | native — sim (no emulator) · silicon **n** low priority |
| **BASIC** | L · native `?` BASIC-52 (N8, investigation) | L · native `?` Tiny BASIC (MIT) on 328P (N8) | L · native — (no permissive interpreter found; MMBasic licence excludes) | L · native — | L · native — | L · **N** MS BASIC ROM (`ms` profile) | L · **N** BBC BASIC (`bbc` profile) | L · native — (N6 measured: 0/35 GW-BASIC sources assemble, no binary) | L |
| **ASM** | **N** hosted sdas8051 · reader **l** (L1) | **N** hosted avr-gcc · **l** | **n** hosted ARM chain exists for `nrf52833` only; add `rp2040` (N4) · **l** | **n** add `stm32f030` (N4) · **l** | **N** hosted `nrf52833` assemble exists; check the ASM tab routes to it (N4) · **l** | **N** hosted ca65 · **l** | **N** hosted sdasz80 · **l** | **N** local, MASM dialect · **l** (L1 starts here) | native — (no emulator) |
| **MicroPython** (tab) | as Python | as Python | as Python | as Python | as Python | as Python | as Python | as Python | as Python |

**Readers into the AST** (the lowered half's precondition): Pseudocode ✓,
Python ✓ (algorithmic subset + STC12/Arduino/Pico/micro:bit pin idioms),
JavaScript ✓, C ✓ (8051 and Arduino headers), BASIC ✓ (BBC subset),
MicroPython ✓, **ASM ✗** — the only language without one, and the deliberate
asymmetry the ASM tab states. L1 turns that into a partial reader with named
refusals rather than a wall.

**Simulator and silicon per device:**

| device | simulator | real silicon |
| --- | --- | --- |
| 8051 STC | emu8051 | STC ISP over Web Serial (cold power-on) |
| AVR Uno/Nano/328P/168P | avr8js | STK500v1 over Web Serial |
| AVR Mega | avr8js | `flashAvrMega` exists in `flasher.js`; `flashFamily` comment says STK500v2 is unimplemented → contradiction, T2 catches it |
| ATtiny85/88 | attiny models | USBasp over WebUSB |
| RP2040 Pico | rp2040js for raw images and compiled C; **MicroPython firmware panics at boot** — measured, see N3 | MicroPython raw REPL over Web Serial (Python tab); C compiles → `/uf2` → `firmware.uf2` for BOOTSEL (C tab), L2 done |
| STM32F030 | CortexM0Machine (light) + labwired (heavy) | UART bootloader over Web Serial; SWD over CMSIS-DAP (WebUSB) |
| micro:bit / Calliope | MicroPython simulator (iframe) | `.hex` append (uflash format), drag-and-drop; DAPLink WebUSB flash **n** (N9) |
| 6502 Eater | W65C02 bench | 28C256 EEPROM burn over Web Serial |
| Z80 bench | Z80 bench | same EEPROM path |
| 8086 DOS bench | i8086 machine, 646k-vector core | none; `.COM`/`.BIN`/floppy-image export **n** (N10) |
| Arcade / PyBadge / SAMD51 | Arcade game console (translated); **no instruction emulator** | CircuitPython `code.py` **n**; PXT UF2 **n** (L4) |
| Arduboy | avr8js console, runs a `.hex` | out of scope (runs, not programs) |

Four contradictions the table already surfaces, carried as sentinels in
`test/bw-matrix-conformance.test.mjs` until T5 fixes them: the AVR
`compile: false` flag versus a hosted compiler the C tab uses; the Mega
STK500v2 comment versus `flashAvrMega`; `CHOOSING-HARDWARE.md` listing compile
targets as "STC12, Pico, STM32F030" while stc-compiler also builds AVR and
6502; and the ASM tab sending `arduino-uno` / `arduino-nano` / `arduino-mega`
to `/assemble` unmapped, where the service knows only MCU ids (`atmega328p`,
`atmega2560`) — the C tab maps them, the ASM tab does not.

---

## 3. Lanes and tasks

Task granularity is one focused agent session. Every task: claim it in
`LANES.md`, keep `npm run test` green **when the machine allows it** (single
files otherwise — see the load rule in `LANES.md`), add licence entries for any
new dependency to `THIRD-PARTY-NOTICES.md` in the same change, and extend a
verify script rather than writing a prose claim. Every DoD line is mechanically
checkable unless marked *(manual)*.

Work that lands in another repository (stc-compiler, bw-board, sb3-creator) is
a task here too: the lite-side half is "bump the pin and prove the cell", and
the row says which repo the other half lives in.

### Lane T — the truth (owner: lego-ac for T1, T2)

**T1. `bw-matrix/capabilities.js` — the table.**
Depends: —.
Schema v1, frozen: `languages[]`, `devices[]` (id, group, family, sim[],
silicon[]), `cells[lang][device] = {native, lowered}` per D1, each fact
carrying `evidence: 'checked' | 'declared'` and, for `native: null`, a
`reason` from the closed set. Pure data plus three lookups: `cell(lang, dev)`,
`overall()`, `explain(lang, dev)` returning the badge text in EN and DE.
DoD:
- [ ] `test/bw-matrix-schema.test.mjs`: every language and device the GUI
      offers has a cell; every `null` native carries a reason from the set;
      no cell is neither native nor lowered.
- [ ] Mutation proof: removing a cell, inventing a reason, or marking a
      declared fact `checked` each turns a named assertion red.

**T2. Conformance gates — the table cannot lie about this repo.**
Depends: T1.
`test/bw-matrix-conformance.test.mjs` reads the sources listed in D2 and
asserts agreement. `scripts/gen-hosted-targets.mjs` snapshots stc-compiler's
`/compile` and `/assemble` target lists into `docs/generated/hosted-targets.json`
with a `--check` mode; the table's hosted facts are checked against the
snapshot, and a nightly workflow step reports (not fails) when the live
service disagrees with the snapshot.
DoD:
- [ ] The three contradictions in §2 are red on the first run (that is the
      proof the gate bites), then fixed by T5 and green.
- [ ] Flip one `compile` flag in `DEVICE_GROUPS`, one `flashFamily` branch,
      one `LOCAL_ASM_TARGETS` entry: each makes a named assertion red.

**T3. Generated doc.**
Depends: T1.
`scripts/gen-language-device-matrix.mjs` → `docs/generated/LANGUAGE-DEVICE-MATRIX.md`,
`--check` wired into `build.yml` beside `gen:8086-report`. Renders §2's two
tables from the module, marks declared-only facts, and lists the open tasks
per cell by id. `CHOOSING-HARDWARE.md` and `device-matrix.md` replace their
hand tables with a link and keep only the prose.
DoD:
- [ ] `npm run gen:matrix:check` green; editing the module without
      regenerating makes it red.
- [ ] No hand-written capability table remains in `docs/*.md` (grep gate for
      the old table headers).

**T4. GUI — overall matrix and contextual badge.**
Depends: T1.
Per D3. Overall view as a modal from the device dropdown; badge beside Run and
Deploy reading `explain(lang, dev)`; both EN and DE; buttons' `disabled`
derived from the same lookup.
DoD:
- [ ] `scripts/verify-matrix-ui.mjs` (playwright): open the modal, count
      cells = languages × devices; select Python + 8051 → badge says lowered
      via C, Deploy enabled; select Python + STM32F030 → badge names the flash
      reason; select ASM + Pico → Assemble disabled with the N4 reason.
- [ ] Bundle: the module is small data, no lazy chunk needed; first-load
      ratchet unchanged (lego-b9's gate).

**T5. Fix what T2 surfaced. DONE 2026-09-05** on branch
`lane/t5-matrix-contradictions` (`8086 coverage testing materials`; branch only,
audited + landed by lego-ac). All five contradictions, each with its sentinel
change in the same commit:
1. AVR `compile: true` for the seven AVR devices, and eater6502 →
   `compile: true, emulator: 'w65c02-bench'` (its shipped engine — the
   `'eater6502'` engine name in the ask was a slip the picker-emulator sentinel
   caught, since picker emulator == matrix engine id). Flipped in DEVICE_GROUPS
   and the matching `dev()`/`avr8` picker fields; the `{contradiction: 'T5'}`
   marker removed from `CELLS.avr.c.native`. z80 stays `compile: false` (awaits
   N1 deploy); arduboy stays `compile: false` (run-.hex console).
2. `flashFamily()` comment reconciled: the Mega's STK500v2 IS implemented
   (`flashAvrMega`) and routes to `avr-mega`, not "unimplemented, falls
   through". Routing unchanged, so the flashFamily sentinels stay green.
3. `CHOOSING-HARDWARE.md` compile-target hand table replaced with a link to the
   generated matrix (T3: no hand-written capability table in docs).
4. `asmTargetForDevice` maps uno/nano → atmega328p, mega → atmega2560 (mirrors
   the C tab's `COMPILE_TARGET`); `KNOWN_UNROUTED_ASM` shrunk to `[]`.
5. STC89C52 kept HOSTED (option b): its matrix override already reads
   `where: 'hosted'`. Promoting it to the local sdcc-wasm needs a RUN of that
   WASM to measure, not done here — inferring it links from its RC sibling would
   be the measure-don't-infer anti-pattern. Documented in CHOOSING-HARDWARE as a
   candidate for a measured local promotion.
DoD: `test/bw-matrix-conformance.test.mjs` 11/11 green; overlay + tracked
`packages/` mirror changed together; the matrix doc regenerated (idempotent —
the changes are picker/marker, not cell values, so the rendered matrix is
unchanged).

### Lane N — native halves to add

**N1. Z80 C via SDCC `-mz80`.** Repo: stc-compiler, then lite. **BUILT 2026-09-05** on stc-compiler branch `lane/z80-c-target` (`6e40fb6e`, delegate, audited by lego-ac: 21 new tests, 432 repo tests green, ten mutation proofs). The vendored SDCC 4.0.0 already had the z80 port; what was missing was `share/sdcc/lib/z80` (crt0 + z80.lib), now vendored from the same .deb the fetch script uses. Map: ROM $0000–$7FFF, RAM $8000–$FFFF from `examples/z80-pd-bench/EXPECTED.md`; `--code-loc 0x0200 --data-loc 0x8000`; stock crt0 (jp init at $0000, SP at $0000 so the first push lands at $FFFE). Also fixed: `stages.py` dropped every exported `GR` symbol (affects 8051 too). **Awaiting merge and deploy by the owner** — the hosted snapshot and the lite `compile: true` flip follow the deploy, not the branch. Lite half of the DoD (bench boot proof: `$0000 == 0xC3`, `latch1.Q0` toggles) is open.
`generateC` already emits a `z80` core (OUT latch on port 0, shadow byte). The
hosted service assembles Z80 (`sdasz80`, `/assemble`) but has **no Z80 C
target** in `TARGETS`. Add `z80` to `TARGETS` with `sdcc -mz80`,
`sdasz80`/`sdldz80`, a bench linker script (ROM at 0x0000, RAM per the Z80
bench part), Intel HEX out. Lite: device `z80` gains `compile: true`, the
image boots in the Z80 bench.
DoD:
- [ ] stc-compiler test: a blink program for the bench compiles and the hex
      has a reset vector at 0x0000.
- [ ] lite: the existing Z80 bench example runs the compiled image in the
      simulator with the LED toggling (playwright, or a node harness on the
      bench machine).
- [ ] Later, N1b: `sdcc-wasm` rebuilt with the z80 port for a local route.

**N2. 8086 C — connect SmallerC to the assembler that already reads NASM.** Lite only. **DONE 2026-09-05** on `lane/language-device-matrix` (delegate, audited by lego-ac): 5 of 5 corpus programs assemble, `requestCBuild`/`cRouteFor` in `bw-asm/assemble-route.js`, ▶ Run C on 8086 in the C tab, an exit-code leak in the WASM glue fixed; `test/smallerc-to-i8086-asm.test.mjs` prints the tally on every run. Not done: a browser gate (box forbids playwright today), `float` support.
`ROADMAP.md` §4.6 says the local assembler "is MASM-dialect and rejects that
output on line 1 (measured 2026-09-04)". **That is stale**: the vendored
`bw-board/i8086-asm.js` carries a source-to-source NASM front end (header line
6, section "The NASM front end", bw-board `df06ddd`, graded byte-identical to
NASM 2.16 on 21 of 31 corpus sources — the rest are `%include` fragments or
constructs NASM itself rejects). What nobody has done is point
`smallerc-wasm`'s output at it. Measure that first; then wire the C tab for
`i8086` to SmallerC → local assembler → DOS bench, and correct the
`smallerc-wasm/compiler.js` header and §4.6.
DoD:
- [ ] `test/smallerc-to-i8086-asm.test.mjs`: every program the SmallerC tests
      already compile assembles through the NASM front end; every failure is
      a named construct, not a parse error on line 1.
- [ ] lite: C `main(){ puts("hi"); }` for `i8086` compiles and assembles
      locally, zero network requests, prints on the DOS bench (playwright).
- [ ] §4.6's exclusion of `smallerc-wasm/compiler.js` is removed because it
      now has a caller; `no-dead-overlay-modules` stays green.

**N3. MicroPython on the Pico, in the simulator.** **MEASURED 2026-09-05, premise inverted**
(delegate, verified independently by lego-ac): MicroPython v1.22.2 **boots to a working REPL**
in rp2040js with the clean-room bootrom — `print(1+1)` answers `2` over the raw-REPL protocol
`pico-repl.js` already speaks. The panic recorded in `rp2040-bootrom.js` was a probe artefact:
the old probe entered at the image's reset vector and skipped boot stage 2, which is what writes
VTOR; the real assert was `irq_set_exclusive_handler` on spinlock 9, and spinlock 20 was read by
`panic()` afterwards. Addresses resolved to pico-sdk functions in `docs/PICO-MICROPYTHON-BOOT.md`;
`scripts/probe-pico-micropython.mjs` reproduces every step; `test/pico-micropython-boot.test.mjs`
(4 tests, named skip without the cached firmware).
**The real gap:** the flash filesystem. Five bootrom table entries (`IF`/`EX`/`FC`/`RE`/`RP`)
miss and the SDK calls the null result, so `deployMainPy` gets `ENODEV`. Fix written and
measured as `docs/bw-board-rp2040-bootrom-flash-funcs.patch` — it belongs in **bw-board**
`src/rp2040-bootrom.js` (vendored here), not in lite.
Remaining, in order:
- [x] N3a (upstream half): landed in bw-board — flash ROM functions `be02550`, reviewed by a
      second session that re-ran the oracle in both directions; `bootFromFlash(image)` on the
      adapter `ff744c5`.
- [ ] N3a (lite half): **blocked on the bw-board pin bump, which is not a file copy, and
      nobody owns it** (checked with brickwright-lite-ea 2026-09-05). What the next person
      starts from: at `ff744c5` `src/target-kinds.js` was CONSOLIDATED, not lost — its two
      exports (`LABWIRED_KIND`, `getTargetKinds()`) now come from `src/debug-target-factory.js`.
      Lite imports the old module from `bw-board/index.js`, `bw-board/debug-target-factory.js`
      and `components/tw-pseudocode/debug-panel.jsx`; the fix is three import paths. The
      hazard is the chunk graph, not the code: the perf lane isolated `target-kinds` in its
      own `bw-debug-target-kinds` chunk, and folding it into `debug-target-factory` (reachable
      from the debugger surface) can re-eager those bytes into first load, whose ratchet is
      9 MB against a measured 7.64. Check emitted chunk names before and after, not only the
      tests. The bump is 17 src files (8086/8088 timing, 8254, 8259, ne2000, reseat-gate,
      debugger); it needs bw-board, the debugger lane and the perf lane at once, and a main
      that can complete a build (none since 11:03 today). The two rp2040 files are
      byte-identical to upstream at the current pin, so N3a's real dependency is narrower
      than the whole bump.
- [x] N3b: `bootFromFlash(image)` on the rp2040js adapter, bw-board `ff744c5` (a second session;
      2/2 tests, stage-2 stub proves execution from flash base). Reaches lite with the pin bump.
- [ ] N3c: lite wiring — the Python tab's ▶ Run for the Pico boots the pinned MicroPython UF2
      (fetched by a `sync:` script with sha256, never committed) and drives the SAME
      `createPicoRepl(transport)` the silicon path uses; `verify:pico-micropython` playwright.

**N4. ARM assembly, hosted — extend the chain that exists.** Repo: stc-compiler, then lite. **stc-compiler half BUILT 2026-09-05** on branch `lane/arm-assemble-rp2040-stm32f030` (`a45ba77`, a second session): `rp2040` (cortex-m0plus, `pico-sram.ld`) and `stm32f030` (cortex-m0, `stm32f030-flash.ld`) beside `nrf52833` in the three maps the arm branch reads; router verified end to end (68 and 84 bytes of Intel HEX), 41/41 in `test_assemble.py`, each assertion mutation-proven. Awaits the owner's merge and deploy; the lite routing half (`asmTargetForDevice` mapping `pico`, `stm32f030`, `microbit`) follows the deploy.
`ASSEMBLE_TARGETS` already routes `nrf52833` through `arm-none-eabi-gcc -x
assembler-with-cpp` with `nrf52833.ld`. Add `rp2040` (`pico-sram.ld`) and
`stm32f030` (`stm32f030-flash.ld`) to the same map. Lite: `asmTargetForDevice`
maps the three devices; the ASM tab's status line says hosted.
DoD: an `.s` that toggles a GPIO assembles for `stm32f030` and runs on the
light tier; the micro:bit ASM tab reaches `nrf52833` (today's routing is
measured first — the map may already cover it).

**N5. JavaScript native on Pico and micro:bit.** Investigation then build.
Pico: Kaluma (Apache-2.0) firmware + its REPL `.load` protocol, mirroring the
MicroPython deploy; micro:bit: PXT static TypeScript (MIT), the compiler
MakeCode itself uses, hosted. Both silicon-first; Pico sim rides N3's boot
path if Kaluma boots in rp2040js.
DoD (investigation): a doc with the boot measurement and licence ledger;
(build): a blink in JS deploys and runs on a real Pico *(manual, recorded)*.

**N6. BASIC native on the 8086 bench.** Investigation. **DONE 2026-09-05, result `native: null`** (a second session, bw-board branch `lane/gw-basic-investigation` `eb19f21`, `docs/GW-BASIC-ON-THE-BENCH.md`): 0 of 35 GW-BASIC (MIT, 2020) sources assemble through `i8086-asm.js` — refusals are MASM's assembly-language layer (COMMENT 19, EXTRN 8, IF1 4, PUBLIC 2, 18 unexpanded macros) plus the unreleased OEM layer the README warns about; the assembler is a single-module subset by design. Unlike the 6502's MS BASIC ROM and the Z80's BBC BASIC image there is no redistributable binary to boot, so the cell is null with that cite, not open.
GW-BASIC's 2020 MIT source is 8088 assembly for MASM; the bench has a MASM
assembler. Measure whether it assembles and boots on the DOS bench; if not,
list what is missing. Alternative candidates must be permissively licensed.
DoD: a doc with the measurement; a LANES row if it turns into a build.

**N7. C on the micro:bit.** Repo: stc-compiler, then lite.
stc-compiler has `nrf52833.ld` and an ARM *assemble* chain for it, but no
`nrf52833` entry in the C compile targets (checked at `79df4b6d`, see
`docs/generated/hosted-targets.json`). Add the compile target beside `rp2040`
and `stm32f030` in `ARM_TARGETS` (bare-metal, `.hex` out), then offer it in
lite: the micro:bit's cell gains a native C half, silicon via the same
drag-and-drop; no simulator (the MicroPython simulator does not run arbitrary
ARM code) — the badge says so.
DoD: the hosted snapshot lists `nrf52833` under compile; the C tab compiles a
blink for it; the matrix cell reads `native · silicon only`.

**N8. BASIC on 8-bit MCUs.** Investigation, low priority.
BASIC-52 on the 8052-class STC parts and a permissively licensed Tiny BASIC on
the ATmega328P. Measure RAM/ROM fit and licence; decide.

**N9. micro:bit flashing over WebUSB (DAPLink).**
`flashSwdStm32` already speaks CMSIS-DAP over WebUSB; the micro:bit's
interface chip is DAPLink. Add the nRF52833 flash algorithm and a button
beside "⬇ .hex for the board".
DoD: mock-DAP unit test of the algorithm; real-board flash *(manual, recorded)*.

**N10. 8086 artefact export.**
`.COM` and a bootable floppy image (`.img`) download for real hardware or
other emulators; not a flash transport, an export. The matrix cell reads
`silicon: export only`.
DoD: the exported `.COM` runs under a second emulator the developer has
(oracle-style, not shipped).

### Lane L — lowered halves to add or complete

**L1. ASM reader with named refusals.** Owner: lego-ac. **v1 LANDED 2026-09-05** (`ea305f40f`) (`lib/bw-asm/asm-8086-to-pseudocode.js`, `test/asm-8086-to-pseudocode.test.mjs`): the emitter's own shapes for the 8086 — the DX:AX stack-machine expression stream, the three comparison templates, and/or/not, set/change/say/print/wait/wait until/repeat/repeat until/if/if-else/forever/stop — read back and re-lower to byte-identical assembly over a five-program corpus; hand-written assembly (every fixture in `examples-i8086.js`) refused as foreign, the scheduler form refused with its script count, pins/ports/displays/tone/PWM/keypad/broadcast/say-for-secs refused by feature name; census (after the scheduler form and the pin family landed on the same branch) 29 lifted / 1 refused / 10 unprobed of 40 anchors, and the lifted set may only grow. Pins and ports read back with declarations synthesised from their use; ACTIVE LOW is not recoverable from the bytes and is warned about (the bytes are identical either way); an INPUT nothing reads is a named refusal because the 8255 control word could not be re-derived. The scheduler form reads back one WHEN per task. The ASM tab's To blocks is wired for the 8086 in source mode. Four mutations proven red. Not yet: keypad, displays, tone, PWM, broadcast, say-for-secs. **Re-scoped:** the 8051 half of L1 is not needed — the only 8051 assembly Brickwright produces is a compiler listing of C that the C tab already reads back; hand-written 8051 assembly is foreign by the same rule as hand-written 8086 assembly.
Lifting, not decompilation: recognise (a) the shapes our own lowering emits
(8086 from `pseudocode-8086.js`, 8051 from SDCC listings of `generateC`
output) and (b) straight-line and single-loop hand-written programs per ISA.
Everything else becomes a `; ASM: <line>` comment plus a warning, the BASIC
reader's doctrine. Start with 8086 (the local assembler already produces a
parse) and 8051.
DoD:
- [ ] Round-trip: every `bw-asm/examples-i8086.js` program lowered by
      `pseudocode-8086.js` reads back to pseudocode that re-lowers to the
      same bytes.
- [ ] A program outside the subset produces N warnings and zero silent drops
      (a counting test on a fixture set).
- [ ] The ASM tab gains "To blocks" with the refusal count in the status
      line; the "deliberate asymmetry" note is replaced by the truthful one.

**L2. Pico C: the silicon half. DONE 2026-09-05** on branch `lane/l2-pico-uf2`
(branch only, for lego-ac's audit).
The C tab already compiles bare-metal RP2040 hosted and runs it in rp2040js
(the device blurb says so, and `compile: true` is correct). Deploy routed the
Pico to MicroPython only. Added: the deploy route branches on artefact kind —
`device === 'pico' && lang === 'c'` → `deployPicoUf2()`, which POSTs the C
source to hosted `/compile` (target `rp2040`, `format: 'bin'`), wraps the bin
with hosted `/uf2` (origin `0x20000000`, the SRAM layout the rp2040 target
links and the sim runs), and offers `firmware.uf2` with a BOOTSEL instruction
(EN/DE). The Python tab still routes to the REPL (`deployToPico`). The matrix
`uf2-bootsel-download` transport flips OPEN→shipped with `flashFamily: null`
(it is dispatched by artefact kind in the handler, not the flasher map) and a
note naming the handler; reach auto-computes (C → uf2, transport accepts uf2)
so the C·Pico and ASM·Pico cells now read `sim + silicon`.
DoD: `test/bw-matrix-conformance.test.mjs` — a node contract test greps the
importer to prove the route sends pico+C to `deployPicoUf2()` and that the
handler reads the C buffer and takes the `/uf2` path (so the cell's `checked`
evidence is earned, not asserted). A browser gate (playwright: C tab → offers
`firmware.uf2`; Python tab → still the REPL) remains for when the box allows
playwright — the same limitation N2 records.

**L3. Reader coverage audit.**
For each reader, a fixture set per device family (Arduino `.ino`, Pico and
micro:bit `.py`, BBC `.bas`) with a measured mapped/refused ratio recorded in
`docs/generated/READER-COVERAGE.md`, generated. Not a coverage target — a
number that stops being folklore.
DoD: the generator runs in CI; ratios are printed, not asserted.

**L4. Arcade lowered to a UF2.** Investigation then build.
`bw_arcade.py` emits PXT TypeScript; the PXT compiler (MIT) can produce a
UF2 for PyBadge. Measure whether it runs in the 250 MB function limit hosted;
if not, name the alternative.
DoD (investigation): doc with the measurement.

### Lane P — peripherals (part profiles), summarised; full detail in its own plan

**P1.** One part-profile registry (bus, pin roles, verbs, host families) with a
gate: every part that has a `bw-board` device model has a profile or an
explicit `programmable: false` reason. **P2.** Split C drivers into a
part-specific protocol layer over a family-specific bus layer; the emitter
half-does this already (`shift_out` has four family variants). **P3.**
MicroPython protocol drivers for the same part set, proven by differential
test: emit C and MicroPython for one program, run both against the simulated
part, compare state; implement the `text_line_0` assert kind, which unblocks
the skipped display assertions in the example corpus. **P4.** Reader-side
library whitelist (`LiquidCrystal`, `Adafruit_SSD1306` → verbs) with named
refusals. **P5.** One silicon wire-truth bench per family *(manual, recorded)*.
Each gets its own LANES row when claimed.

---

## 4. Milestones

**M1 — Truth (T1–T5).** The GUI shows what runs where, the docs are generated
from the same module, and the three known contradictions are fixed. *Exit:*
`verify:matrix-ui` and `gen:matrix:check` green in CI.

**M2 — Cheap native closures (N1, N2, N4, N7, L2).** Z80 and 8086 gain C;
ARM gains ASM; the micro:bit gains C; the Pico's C path is reachable. *Exit:*
each cell's DoD green; the matrix regenerated.

**M3 — Simulator parity (N3).** Python on the Pico runs in the simulator as
it does on silicon. *Exit:* `verify:pico-micropython` green.

**M4 — The last language gap (L1) and reader truth (L3).** ASM reads back
with counted refusals. *Exit:* L1 DoD green.

**M5 — Investigations decided (N5, N6, N8, L4).** Each ends in a doc with a
measurement and either a build task or a `native: null` reason the table
cites.

**M6 — Peripherals (Lane P)**, sequenced after M1 so the part table joins a
matrix that already exists.

Parallelism: T1 first (everything renders from its schema). Then N1, N2, N3
are independent of each other and of T3/T4, and are the first wave for
delegated agents — N2 first because it is now measurement plus wiring. L1 is the long pole and stays with the owner.

## 5. Non-goals — write refusals, not scope creep

- A Python or JavaScript interpreter on the 8051, AVR, STM32F030, 6502, Z80 or
  8086. The table says `native: null` with the RAM or port reason; lowered
  covers the cell.
- A SAMD51 instruction emulator. Upstream Rust work in labwired; not here.
- Arduino library compatibility in general. Known APIs map to verbs (P4);
  the rest is a named refusal.
- General decompilation. L1 lifts known shapes and says what it did not lift.

## 6. Risks and tripwires

| risk | tripwire | response |
| --- | --- | --- |
| The table drifts from the code | T2 gates, mutation-proven | a red build, by name |
| Hosted service changes under us | nightly snapshot diff (T2) | report, then bump the snapshot deliberately |
| Machine load kills long test runs | `scripts/check-system-load.mjs` | single-file tests; never a full suite on a loaded box |
| A "physically impossible" claim is wrong | every `null` reason cites a number | fix the number, the cell opens |
| Two sessions close the same cell | LANES rows per task id | the row is the lock |

## 7. Licence ledger for new runtimes this plan may add

| component | use | licence |
| --- | --- | --- |
| MicroPython firmware (RP2040 UF2) | N3, fetched by sync script | MIT |
| Kaluma | N5 candidate | Apache-2.0 |
| Espruino | N5 candidate | MPL-2.0 |
| PXT (MakeCode compiler) | N5, L4, hosted | MIT |
| GW-BASIC (2020 source) | N6 candidate | MIT |
| SDCC z80 port, arm-none-eabi binutils, cc65 | hosted only | GPL, never bundled |
| NASM | developer oracle for N2 | BSD-2-Clause, not shipped |
