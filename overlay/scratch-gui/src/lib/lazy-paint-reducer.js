// Keep the reducer stage independent from scratch-paint's public index: that
// index also imports the editor and collapses the two activation boundaries.
import ScratchPaintReducer from 'scratch-paint/src/reducers/scratch-paint-reducer';

export default ScratchPaintReducer;
