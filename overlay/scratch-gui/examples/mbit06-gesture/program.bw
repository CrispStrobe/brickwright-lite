DEVICE MICROBIT

# Gesture demo: shake to roll a die, tilt to show arrows
WHEN flag clicked:
  show text "?"
  FOREVER:
    set variable g to read accel strength
    if g > 1500 then
      set variable die to pick random 1 to 6
      show text die
      wait 1 seconds
    wait 0.1 seconds
